---
import BaseLayout from '@/layouts/BaseLayout.astro';
const base = import.meta.env.BASE_URL.replace(/\/$/, '');
---

<BaseLayout title="自分を作れ / Build Yourself First / Grappling Primitives" fullscreen={true}>

<!-- ── Toolbar ──────────────────────────────────────────────────────────────── -->
<div id="toolbar" style="position:fixed;top:0;left:0;right:0;height:36px;z-index:50;display:flex;align-items:center;gap:8px;padding:0 12px;border-bottom:1px solid #21262d;background:rgba(5,5,9,0.97);backdrop-filter:blur(8px);font-family:'Courier New',Consolas,Monaco,monospace;">
  <a href={`${base}/`} style="font-size:10px;letter-spacing:0.2em;color:#6b7280;text-transform:uppercase;text-decoration:none;" onmouseover="this.style.color='#00d4ff'" onmouseout="this.style.color='#6b7280'">&larr; Home</a>
  <span style="color:#21262d;">|</span>
  <span style="font-size:11px;letter-spacing:0.2em;color:#6b7280;text-transform:uppercase;">自分を作れ</span>
  <div style="flex:1;"></div>
  <button id="deeplink-btn" style="font-family:inherit;font-size:11px;letter-spacing:0.1em;text-transform:uppercase;padding:3px 10px;border-radius:3px;cursor:pointer;border:1px solid #21262d;background:transparent;color:#6b7280;transition:all 0.15s;" onmouseover="this.style.borderColor='rgba(0,212,255,0.4)';this.style.color='#00d4ff'" onmouseout="if(this.textContent==='Link'){this.style.borderColor='#21262d';this.style.color='#6b7280'}">Link</button>
  <button id="lang-toggle" style="font-family:inherit;font-size:9px;letter-spacing:0.16em;text-transform:uppercase;padding:3px 10px;border-radius:3px;cursor:pointer;border:1px solid #21262d;background:transparent;color:#6b7280;">
    <span class="lang-label" data-active="en">EN</span> / <span class="lang-label">JP</span>
  </button>
</div>

<!-- ── Scrolling Content ───────────────────────────────────────────────────── -->
<div style="padding-top:36px;">

<!-- ═══════════════════════════════════════════════════════════════════════════ -->
<!-- ACT 0 — Hero                                                              -->
<!-- ═══════════════════════════════════════════════════════════════════════════ -->
<section id="act-0" class="act" data-act="0">
  <div class="act-inner hero-layout">
    <blockquote class="hero-quote">
      <p>
        <span lang="en">"If you could tell your white belt self one thing, what would it be?"</span>
        <span lang="ja">「白帯の自分に一つだけ伝えられるとしたら、何を伝える？」</span>
      </p>
    </blockquote>

    <div class="hero-center">
      <h2 class="hero-subtitle">
        <span lang="en">Build<br>Yourself<br>First</span>
        <span lang="ja">まず<br>自分を<br>作れ</span>
      </h2>
      <img class="hero-calligraphy" src="/images/jibunwotsukure/calligraphy.png" alt="自分を作れ calligraphy" />
    </div>

    <p class="act-subtitle">
      <span lang="en">The answer isn't more techniques, it is your 'body state'.<br>That is the most essential variable.</span>
      <span lang="ja">答えは技を増やすことではない。身体の状態こそが最も重要で本質的な変数。</span>
    </p>
  </div>
</section>

<!-- ═══════════════════════════════════════════════════════════════════════════ -->
<!-- ACT 1 — The Source: Kodokan                                               -->
<!-- ═══════════════════════════════════════════════════════════════════════════ -->
<section id="act-1" class="act" data-act="1">
  <div class="act-inner">
    <h2 class="act-title"><span class="act-number">01</span><span lang="en">The Source / 技の原理</span><span lang="ja">源流 / 技の原理</span></h2>
    <p class="act-lead">
      <span lang="en">Kano Jigoro, the founder of Judo, described the principle of technique (<em>waza no genri</em>) as two parts: <strong>Tsukuri</strong> (set-up) and <strong>Kake</strong> (execution). But buried inside Tsukuri is an instruction that gets lost in translation. Tsukuri itself is made up of two elements: "Kuzushi" and "Jibun wo Tsukuru". In my experience of beginners, and myself years ago, we tend to focus so much on off-balancing and disrupting the opponent that we do so at the expense of ourselves.</span>
      <span lang="ja">柔道の創始者・嘉納治五郎は技の原理を「作り」と「掛け」の二部構成として説いた。しかし「作り」の中に、翻訳で失われがちな重要な指示が埋もれている。作りそのものは「崩し」と「自分を作る」の二つの要素からできている。初心者の頃、そして過去の自分自身を見ると、相手を崩すことに集中しすぎて、自分自身を犠牲にしてしまう傾向がある。</span>
    </p>

    <div class="source-buttons">
      <button class="source-btn" id="btn-kodokan" aria-label="View Kodokan original text">
        <span class="source-btn-label">
          <span lang="en">Kodokan Original Text</span>
          <span lang="ja">講道館 原文</span>
        </span>
        <span class="source-btn-sub">kdkjudo.org / 技の原理</span>
      </button>
      <button class="source-btn source-btn--alt" id="btn-kuzushi-xyz" aria-label="View kuzushi.xyz translation">
        <span class="source-btn-label">
          <span lang="en">kuzushi.xyz Translation</span>
          <span lang="ja">kuzushi.xyz 翻訳</span>
        </span>
        <span class="source-btn-sub">2019 alternative reading</span>
      </button>
    </div>

    <div class="reframe-callout">
      <div class="reframe-arrow">&darr;</div>
      <div class="reframe-content">
        <p class="reframe-from">
          <span lang="en">"<s>holding oneself ready</s>"</span>
          <span lang="ja">"<s>準備を整える</s>"</span>
        </p>
        <div class="reframe-arrow">&darr;</div>
        <p class="reframe-to">
          <span lang="en">"<strong>Building Oneself</strong>" / 自分を作る</span>
          <span lang="ja">「<strong>自分を作る</strong>」/ Jibun wo Tsukuru</span>
        </p>
      </div>
    </div>

    <p class="act-lead" style="margin-top:24px;">
      <span lang="en">This is not passive waiting, and this is not simply "posture" either. It is <em>active construction</em>. And "posture" (姿勢) is so much more than standing upright, it is the full organization of your body as a force-generating, load-bearing, dynamically stable system.</span>
      <span lang="ja">これは受動的な待機ではなく、単なる「姿勢」でもない。<em>能動的な構築</em>である。そして「姿勢」とは単に背筋を伸ばすことではなく、力を生み出し、荷重を支え、動的に安定するシステムとして身体を組織化することである。</span>
    </p>
  </div>
</section>

<!-- ── Modal A1: Kodokan Text ────────────────────────────────────────────── -->
<div id="modal-kodokan" class="modal-overlay" role="dialog" aria-label="Kodokan original text">
  <div class="modal-panel">
    <div class="modal-header">
      <div>
        <div class="modal-title">技の原理 / The Principle of Technique</div>
        <div class="modal-subtitle">Kodokan Judo / Official Source</div>
      </div>
      <button class="modal-close" aria-label="Close">&times;</button>
    </div>
    <div class="modal-body">
      <h3 class="modal-section-title">原文 (Japanese Original)</h3>
      <div class="modal-jp-text">
「身体と精神を最も有効に働かせる」、これが柔道の根本原理で、この原理を技の上に生かしたのが「作り」と「掛け」の理論となります。

「作り」は、相手の体を不安定にする「くずし」と、自分の体が技を施すのに最も良い位置と姿勢をとる「自分を作る」ことから成り立っています。「掛け」は、この作られた一瞬に最後の決め手を施すことをいいます。

この「作り」と「掛け」は、柔道の根本原理に従った技術原理ということができます。

互いに、精力善用・自他共栄の根本原理に即した作りと掛けを競い合う間に、自然とこの根本原理を理解し、体得して、社会百般の実生活に生かそうとしています。

「技から道に入る」わけです。
      </div>

      <h3 class="modal-section-title" style="margin-top:24px;">Official English Translation (Kodokan)</h3>
      <div class="modal-en-text">
"Making the most effective use of body and mind" is the fundamental principle of Judo, and it is this principle applied to technique that gives us the theories of Tsukuri (set-up) and Kake (execution).

Tsukuri consists of Kuzushi (breaking the opponent's balance) and positioning one's own body in the best posture and position for applying the technique, "holding oneself ready."

Kake is the application of the decisive finishing move at that prepared instant.

Tsukuri and Kake can be called the technical principles that follow the fundamental principles of Judo.

As we compete with each other through Tsukuri and Kake in accordance with the fundamental principles of Seiryoku Zenyo (maximum efficiency) and Jita Kyoei (mutual welfare and benefit), we naturally come to understand and embody these principles, seeking to apply them in all aspects of daily life.

"From technique, we enter the Way."
      </div>

      <div class="modal-attribution">
        <a href="https://kodokanjudoinstitute.org/doctrine/" target="_blank" rel="noopener">kodokanjudoinstitute.org/doctrine/</a><br/>
        <a href="https://www.kodokan.org/e_judo/GokPrin.html" target="_blank" rel="noopener">kodokan.org / Principles</a>
      </div>
    </div>
  </div>
</div>

<!-- ── Modal A2: kuzushi.xyz Translation ────────────────────────────────── -->
<div id="modal-kuzushi-xyz" class="modal-overlay" role="dialog" aria-label="kuzushi.xyz translation">
  <div class="modal-panel">
    <div class="modal-header">
      <div>
        <div class="modal-title">The Principle of Waza</div>
        <div class="modal-subtitle">www.kuzushi.xyz / 2019 Translation</div>
      </div>
      <button class="modal-close" aria-label="Close">&times;</button>
    </div>
    <div class="modal-body">
      <div class="modal-vocab">
        <div class="vocab-item"><strong>崩し</strong> Kuzushi, from 崩す (kuzusu): destroy, tear down, pull apart, lose balance</div>
        <div class="vocab-item"><strong>作る</strong> Tsukuru: make, build, produce, establish, set-up, form</div>
        <div class="vocab-item"><strong>掛ける</strong> Kakeru: start (an engine), catch (in a net/trap), put (a burden) on</div>
      </div>

      <div class="modal-translation-grid">
        <div class="translation-row"><span class="tl-jp">「崩し」</span><span class="tl-action">Break | Disrupt</span><span class="tl-target">the opponent</span></div>
        <div class="translation-row"><span class="tl-jp">「作り」</span><span class="tl-action">Set | Prepare</span><span class="tl-target">your own body</span></div>
        <div class="translation-row"><span class="tl-jp">「掛け」</span><span class="tl-action">Do | Execute</span><span class="tl-target">the technique</span></div>
      </div>

      <h3 class="modal-section-title" style="margin-top:24px;">Translation</h3>
      <div class="modal-en-text">
The "optimal use of body and mind" is a foundational principle of Judo, which comes to life through the theories of Tsukuri (set-up) and Kake (performing the technique).

<strong>Tsukuri is made of both breaking the balance of the other (Kuzushi) and setting up one's own body with the best position and posture (Jibun wo tsukuru, Building oneself).</strong>

Kake (perform) is what we call the culmination of that one instant which we set-up.

We can call Tsukuri and Kake the technical principles that follow the basic principles of Judo.
      </div>

      <div class="modal-mnemonic">
        <span lang="en">Proposed mnemonics:</span>
        <span lang="ja">提案する記憶法：</span>
        <div class="mnemonic-list">
          <div>break – set – catch</div>
          <div>[break~set] – do</div>
          <div>[set~break] – execute</div>
          <div style="color:var(--text-dim);font-size:0.85em;margin-top:4px;">
            <span lang="en">(the order of the first two is interchangeable)</span>
            <span lang="ja">（最初の二つの順序は入れ替え可能）</span>
          </div>
        </div>
      </div>

      <div class="modal-attribution">
        <a href={`${base}/modules/articles/kuzushi-tsukuri-kake-the-doctrine-and-purpose-of-judo`}>Read the full article &rarr;</a>
      </div>
    </div>
  </div>
</div>

<!-- ═══════════════════════════════════════════════════════════════════════════ -->
<!-- ACT 2 — Build Yourself                                                    -->
<!-- ═══════════════════════════════════════════════════════════════════════════ -->
<section id="act-2" class="act" data-act="2">
  <div class="act-inner">
    <h2 class="act-title"><span class="act-number">02</span><span lang="en">自分を作れ / Build Yourself</span><span lang="ja">自分を作れ / Build Yourself</span></h2>
    <p class="act-lead">
      <span lang="en">Your own body state is the real variable. That is the key insight, the 閃き.</span>
      <span lang="ja">あなた自身の身体の状態こそが真の変数。それが閃き、key insightである。</span>
    </p>
    <p class="act-lead">
      <span lang="en">What separates an experienced grappler from a beginner isn't a larger catalog of techniques, it's an invisible quality of body. Some call it "invisible jiu-jitsu." We call it <em>self-organization</em>.</span>
      <span lang="ja">経験者と初心者を分けるのは技のカタログの大きさではなく、目に見えない身体の質である。「見えない柔術」とも呼ばれるが、我々はそれを<em>身体の自己組織化</em>と呼ぶ。</span>
    </p>

    <div class="four-dimensions">
      <div class="dimension-card">
        <div class="dimension-number">1</div>
        <h3><span lang="en">Tension Economy</span><span lang="ja">張力の経済性</span></h3>
        <div class="dimension-jp">無駄を省く</div>
        <p><span lang="en">Efficient vs "parasitic" tension. The idea is to realize the minimum amount of force you must use to achieve your goal, be it keeping a frame or making the other move or applying enough force to move a lever. At the extreme, anything beyond the bare minimum is wasted energy.</span><span lang="ja">効率的な張力と「寄生的」な張力。フレームの維持であれ、相手を動かすことであれ、レバーを動かすのに十分な力であれ、目標達成に必要な最小限の力を認識すること。極端に言えば、必要最小限を超えるものは全てエネルギーの浪費である。</span></p>
      </div>
      <div class="dimension-card">
        <div class="dimension-number">2</div>
        <h3><span lang="en">Internal Connections</span><span lang="ja">内部接続</span></h3>
        <div class="dimension-jp">體の統一</div>
        <p><span lang="en">"Kinetic chains" is a concept from mechanical engineering later applied to human movement. We need to figure out how open and closed kinetic chains work in our bodies. "Don't think, feel" is not just voodoo kung-fu bullshit. There's science behind "connection" and neuro-muscular paths.</span><span lang="ja">「運動連鎖」は機械工学の概念で、後に人体の動きに応用された。開放型と閉鎖型の運動連鎖が身体の中でどう機能するかを理解する必要がある。「考えるな、感じろ」は武術のオカルトではない。「接続」と神経筋経路には科学的根拠がある。</span></p>
      </div>
      <div class="dimension-card">
        <div class="dimension-number">3</div>
        <h3><span lang="en">Force Flow / Ground Path</span><span lang="ja">力の流れ / 地面への経路</span></h3>
        <div class="dimension-jp">地面への道</div>
        <p><span lang="en">Force transmission to ground. Clean pathways from contact point through skeleton to base.</span><span lang="ja">地面への力の伝達。接点から骨格を通じて基部へのクリーンな経路。</span></p>
      </div>
      <div class="dimension-card">
        <div class="dimension-number">4</div>
        <h3><span lang="en">Stacking Alignment</span><span lang="ja">積み重ね整列</span></h3>
        <div class="dimension-jp">部位の整列</div>
        <p><span lang="en">Skeletal load-bearing. Stack bones so muscles do less, structure does more.</span><span lang="ja">骨格による荷重支持。骨を積み重ね、筋肉の負担を減らし、構造に任せる。</span></p>
      </div>
    </div>

    <div class="build-yourself-inline" id="by-viz">
      <!-- Controls -->
      <div class="by-controls">
        <button id="by-autoplay" class="by-play-btn" aria-label="Pause auto-advance"><span class="by-play-icon">⏸</span></button>
        <span class="by-label-end">Unbuilt</span>
        <input type="range" id="by-slider" min="0" max="1" step="0.001" value="0" class="by-slider" aria-label="Build progress" />
        <span class="by-label-end">Built</span>
      </div>
      <div id="by-slider-label" class="by-slider-label"></div>
      <div id="by-steps" class="by-steps"></div>
      <!-- 4 Canvas Panels -->
      <div id="by-grid" class="by-grid">
        <div class="by-panel"><canvas id="by-canvas-tension"></canvas></div>
        <div class="by-panel"><canvas id="by-canvas-connections"></canvas></div>
        <div class="by-panel"><canvas id="by-canvas-force"></canvas></div>
        <div class="by-panel"><canvas id="by-canvas-stacking"></canvas></div>
      </div>
    </div>

    <p class="disclaimer">
      <span lang="en">Disclaimer! This is <strong>NOT</strong> scientific at all, it is mostly what it 'feels' like to neither collapse under pressure nor keeping the opponent at bay by muscling through, heavily influenced by my time training at Aunkai with Akuzawa-sensei. He once joked I understood "17%" of what he does with his kobujutsu inspired research. Amazing teacher.</span>
      <span lang="ja">免責事項！これは<strong>科学的ではまったくありません</strong>。プレッシャーで身体が崩れる感覚、力で相手を押さえ込む感覚、あるいはより神秘的でリラックスした太極拳のような「内的」構造を組み立てる感覚の違いを、主観的に表現したものです。</span>
    </p>
  </div>
</section>

<!-- ═══════════════════════════════════════════════════════════════════════════ -->
<!-- ACT 3 — f(Tsukuri)                                                        -->
<!-- ═══════════════════════════════════════════════════════════════════════════ -->
<section id="act-3" class="act" data-act="3">
  <div class="act-inner">
    <h2 class="act-title"><span class="act-number">03</span><span lang="en">ƒ(Tsukuri) / The Building Blocks</span><span lang="ja">ƒ(作り) / 構築の要素</span></h2>
    <p class="act-lead">
      <span lang="en">"Tsukuri" can be unpacked into so many building blocks. Here's one model. "All models are wrong, some are useful."</span>
      <span lang="ja">「作り」は多くの構成要素に分解できる。これは一つのモデル。「全てのモデルは間違っている。しかし有用なものもある。」</span>
    </p>
    <div class="viz-zoomable">
      <div id="viz-tsukuri" class="viz-container"></div>
      <p class="zoom-hint"><span lang="en">Pinch to zoom</span><span lang="ja">ピンチでズーム</span></p>
    </div>
  </div>
</section>

<!-- ═══════════════════════════════════════════════════════════════════════════ -->
<!-- ACT 4 — f(Kuzushi)                                                        -->
<!-- ═══════════════════════════════════════════════════════════════════════════ -->
<section id="act-4" class="act" data-act="4">
  <div class="act-inner">
    <h2 class="act-title"><span class="act-number">04</span><span lang="en">ƒ(Kuzushi) / The Disruption</span><span lang="ja">ƒ(崩し) / 破壊の要素</span></h2>
    <p class="act-lead">
      <span lang="en">Kuzushi is used in English as a shortcut for "imbalance" or "disrupting" the other grappler. But I also find it helpful to unpack what it is made of, to recognize the various elements that can create that destabilization. Some food for thought.</span>
      <span lang="ja">崩しは英語では「不均衡」や「相手の撹乱」の略語として使われる。しかしその構成要素を分解し、不安定化を生み出す様々な要素を認識することも有用だと思う。考えるきっかけになれば。</span>
    </p>
    <div class="viz-zoomable">
      <div id="viz-kuzushi" class="viz-container"></div>
      <p class="zoom-hint"><span lang="en">Pinch to zoom</span><span lang="ja">ピンチでズーム</span></p>
    </div>
  </div>
</section>

<!-- ═══════════════════════════════════════════════════════════════════════════ -->
<!-- ACT 5 — How They Connect                                                  -->
<!-- ═══════════════════════════════════════════════════════════════════════════ -->
<section id="act-5" class="act" data-act="5">
  <div class="act-inner">
    <h2 class="act-title"><span class="act-number">05</span><span lang="en">How They Connect</span><span lang="ja">それらはどう繋がるか</span></h2>
    <p class="act-lead">
      <span lang="en">The sequence most people focus on is Kuzushi → Tsukuri → Kake. imho you hear many more advocations to "break their posture!" and "kuzushi first before technique!" than "focus on building yourself". Actually we hear it in the defensive sense; someone has a triangle set-up on you? "Posture up!". My take is that building-yourself is not defensive-only, it makes for a better base of offense too. I believe that explicitly focusing on it can enhance one's skills. Body-skills are a multiplier for both better defense and offense.</span>
      <span lang="ja">多くの人が注目する順序は 崩し → 作り → 掛け。「姿勢を崩せ！」「技の前にまず崩し！」という声は「自分を作ることに集中しろ」よりもずっと多く聞こえる。実際、防御的な意味では聞く。三角絞めをセットアップされたら？「姿勢を正せ！」と。私の考えでは、自分を作ることは防御だけではなく、攻撃のより良い土台にもなる。意識的にそこに集中することで技術を高められると信じている。身体のスキルは防御と攻撃の両方の乗数となる。</span>
    </p>
    <div id="viz-flow" class="viz-container"></div>

    <p class="act-lead" style="margin-top:24px;">
      <span lang="en">Both Jibun wo Tsukuru (building yourself) and Kuzushi (disrupting the opponent) are components of <strong>Tsukuri</strong> (the set-up). When both pre-requisites are met, the conditions exist to attempt <strong>Kake</strong> (execution).</span>
      <span lang="ja">自分を作る（自己構築）と崩し（相手の撹乱）は共に<strong>作り</strong>（セットアップ）の構成要素。両方の前提条件が揃った時、<strong>掛け</strong>（実行）を試みる条件が整う。</span>
    </p>

    <div class="examples-box">
      <h3><span lang="en">Theory → Practice</span><span lang="ja">理論 → 実践</span></h3>
      <div class="example-item">
        <span class="example-label"><span lang="en">Guard pass:</span><span lang="ja">パスガード：</span></span>
        <span lang="en">Build your posture and grips (Tsukuri) → strip their frames and off-balance (Kuzushi) → pass (Kake)</span>
        <span lang="ja">姿勢とグリップを構築（作り）→ フレームを剥がし崩す（崩し）→ パス（掛け）</span>
      </div>
      <div class="example-item">
        <span class="example-label"><span lang="en">Sweep:</span><span lang="ja">スイープ：</span></span>
        <span lang="en">Elevate their weight off their base (Kuzushi) → load your hips underneath (Tsukuri) → sweep (Kake)</span>
        <span lang="ja">相手のベースから体重を浮かす（崩し）→ 腰を下に入れる（作り）→ スイープ（掛け）</span>
      </div>
      <div class="example-item">
        <span class="example-label"><span lang="en">Armbar:</span><span lang="ja">腕十字：</span></span>
        <span lang="en">Isolating the arm is part of Kuzushi. Breaking their posture is another part of Kuzushi, to prevent their recovery. Not overextending yourself while attacking is part of Jibun wo Tsukuru, Building Yourself. Together: 1) Kuzushi + 2) Jibun wo Tsukuru = 3) ready to execute the technique from a position of strength against an opponent structurally unable to defend.</span>
        <span lang="ja">腕を孤立させることは崩しの一部。姿勢を崩すことも崩しの一部で、回復を防ぐため。攻撃中に自分を伸ばしすぎないことは自分を作るの一部。合わせて：1) 崩し + 2) 自分を作る = 3) 構造的に防御できない相手に対して、強い位置から技を実行する準備が整う。</span>
      </div>
    </div>
  </div>
</section>

<!-- ═══════════════════════════════════════════════════════════════════════════ -->
<!-- ACT 6 — The Multiplier Effect                                             -->
<!-- ═══════════════════════════════════════════════════════════════════════════ -->
<section id="act-6" class="act act--cta" data-act="6">
  <div class="act-inner cta-layout">
    <div class="cta-center">
      <div class="cta-left">
        <h2 class="cta-title">
          <span lang="en">The<br><span style="color:var(--cyan);text-shadow:var(--glow-cyan);">Multiplier</span><br>Effect</span>
          <span lang="ja"><span style="color:var(--cyan);text-shadow:var(--glow-cyan);">乗数</span><br>効果</span>
        </h2>
        <p class="cta-lead">
          <span lang="en">A strong "Build Yourself" base is a multiplier for all techniques. Every sweep, pass, and submission works better when your body is organized first.</span>
          <span lang="ja">強力な「自分を作る」基盤は全ての技の乗数となる。スイープもパスもサブミッションも、身体が先に組織化されていればより良く機能する。</span>
        </p>
      </div>
      <img class="cta-calligraphy" src="/images/jibunwotsukure/shingitai.png" alt="心技体 Shin Gi Tai calligraphy" />
    </div>

    <p class="cta-bottom-right">
      <span lang="en">One of the fastest ways to develop those skills is to have <a href={`${base}/modules/ideal-partner`} class="inline-link">ideal training partners</a> and to become one yourself. Another is to start filling your mind with the Grappling Primitives that underlie all techniques, with the building blocks of which every complex technique is made of. Explore these in the <a href={`${base}/modules/quadtree`} class="inline-link">QuadTree concept map</a>.</span>
      <span lang="ja">これらのスキルを最速で身につける方法の一つは、<a href={`${base}/modules/ideal-partner`} class="inline-link">理想のトレーニングパートナー</a>を持ち、自分自身がそうなることだ。もう一つは、全ての技の根底にあるグラップリングの基本要素、あらゆる複雑な技を構成するビルディングブロックで頭を満たすこと。<a href={`${base}/modules/quadtree`} class="inline-link">QuadTreeコンセプトマップ</a>で探索しよう。</span>
    </p>
  </div>
</section>

</div><!-- end scrolling content -->

</BaseLayout>

<!-- ═══════════════════════════════════════════════════════════════════════════ -->
<!-- STYLES                                                                    -->
<!-- ═══════════════════════════════════════════════════════════════════════════ -->
<style is:global>
:root {
  --bg:           #050509;
  --surface:      #0E1014;
  --border:       #1C1F26;
  --accent:       #4C8DFF;
  --accent-soft:  #A970FF;
  --cyan:         #00FFFF;
  --cyan-dim:     #00d4ff;
  --green:        #34D399;
  --green-dim:    #16a34a;
  --amber:        #FBBF24;
  --orange:       #ea580c;
  --orange-dim:   #f97316;
  --red:          #F87171;
  --text-primary: #E5E7EB;
  --text-muted:   #9CA3AF;
  --text-dim:     #6B7280;
  --font-mono:    'Courier New', Consolas, Monaco, monospace;
  --glow-cyan:    0 0 12px #00FFFF88;
  --glow-blue:    0 0 12px #4C8DFF88;
  --glow-soft:    0 0 8px #A970FF66;
  --glow-green:   0 0 12px #34D39988;
  --glow-orange:  0 0 12px #ea580c88;
}

/* Hide JP by default */
[lang="ja"] { display: none; }
body.lang-ja [lang="ja"] { display: inline; }
body.lang-ja [lang="en"] { display: none; }

/* Toolbar lang toggle active state */
.lang-label[data-active] { color: #00FFFF; text-shadow: 0 0 12px #00FFFF88; }

/* ─── Acts ─── */
.act {
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 80px 24px;
  border-bottom: 1px solid var(--border);
  opacity: 0;
  transform: translateY(16px);
  transition: opacity 0.5s ease-out, transform 0.5s ease-out;
}
.act.visible {
  opacity: 1;
  transform: translateY(0);
}
.act-inner {
  max-width: 860px;
  width: 100%;
}
.act-number {
  display: block;
  font-size: 11px;
  letter-spacing: 0.3em;
  color: var(--text-dim);
  margin-bottom: 4px;
}
.act-title {
  font-size: clamp(1.4rem, 4vw, 2.2rem);
  font-weight: 400;
  color: var(--text-primary);
  margin-bottom: 16px;
  text-shadow: var(--glow-blue);
}
.act-lead {
  color: var(--text-muted);
  font-size: var(--text-base);
  margin-bottom: 32px;
  max-width: 680px;
  line-height: 1.8;
}
.act-subtitle {
  color: var(--text-muted);
  font-size: 0.95rem;
  margin-top: 24px;
}
.act-subtitle em, .act-lead em {
  color: var(--cyan);
  font-style: normal;
  text-shadow: var(--glow-cyan);
}

/* ─── Hero (Act 0) ─── */
.hero-layout {
  display: flex;
  flex-direction: column;
  align-items: center;
  text-align: center;
  gap: 0;
}
.hero-center {
  position: relative;
  width: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 24px 0;
}
.hero-calligraphy {
  width: clamp(280px, 50vw, 520px);
  height: auto;
  opacity: 0.85;
  pointer-events: none;
  user-select: none;
}
.hero-subtitle {
  position: absolute;
  left: 0;
  width: 50%;
  text-align: center;
  font-size: clamp(1.4rem, 3.5vw, 2.2rem);
  font-weight: 400;
  color: var(--text-primary);
  letter-spacing: 0.18em;
  text-transform: uppercase;
  line-height: 1.6;
  margin: 0;
  z-index: 1;
}

/* ─── Hero Quote ─── */
.hero-quote {
  text-align: center;
  padding: 32px 16px;
  border-left: none;
}
.hero-quote p {
  font-size: clamp(1.1rem, 3vw, 1.5rem);
  color: var(--text-primary);
  font-style: italic;
  line-height: 1.5;
  text-shadow: var(--glow-soft);
}
.hero-quote cite {
  display: block;
  margin-top: 12px;
  font-size: 0.8rem;
  color: var(--text-dim);
  font-style: normal;
}

/* ─── Source Buttons (Act 1) ─── */
.source-buttons {
  display: flex;
  gap: 12px;
  margin: 32px 0;
  flex-wrap: wrap;
}
.source-btn {
  flex: 1;
  min-width: 220px;
  padding: 16px 20px;
  border: 1px solid var(--border);
  border-radius: 6px;
  background: var(--surface);
  color: var(--text-primary);
  cursor: pointer;
  text-align: left;
  font-family: var(--font-mono);
  transition: all 0.2s;
}
.source-btn:hover {
  border-color: var(--cyan-dim);
  box-shadow: 0 0 16px rgba(0,212,255,0.1);
}
.source-btn--alt:hover {
  border-color: var(--orange-dim);
  box-shadow: 0 0 16px rgba(249,115,22,0.1);
}
.source-btn-label {
  display: block;
  font-size: 0.95rem;
  font-weight: 600;
  margin-bottom: 4px;
}
.source-btn-sub {
  display: block;
  font-size: 0.75rem;
  color: var(--text-dim);
  letter-spacing: 0.05em;
}

/* ─── Reframe Callout ─── */
.reframe-callout {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: 6px;
  padding: 24px;
  text-align: center;
  margin: 32px 0;
}
.reframe-arrow {
  color: var(--cyan-dim);
  font-size: 1.5rem;
  margin: 8px 0;
}
.reframe-from {
  font-size: 1rem;
  color: var(--text-dim);
}
.reframe-from s {
  color: var(--red);
}
.reframe-to {
  font-size: 1.1rem;
  color: var(--cyan);
  text-shadow: var(--glow-cyan);
}

/* ─── Modals ─── */
.modal-overlay {
  display: none;
  position: fixed;
  inset: 0;
  z-index: 200;
  align-items: center;
  justify-content: center;
  padding: 16px;
  background: rgba(0,0,0,0.80);
  backdrop-filter: blur(4px);
}
.modal-overlay.active {
  display: flex;
}
.modal-panel {
  width: 100%;
  max-width: min(700px, 98vw);
  max-height: 88vh;
  display: flex;
  flex-direction: column;
  font-family: var(--font-mono);
  background: #000d18;
  border: 1px solid rgba(132,138,148,0.53);
  box-shadow: 0 0 40px rgba(0,0,0,0.8);
  border-radius: 4px;
}
.modal-header {
  display: flex;
  align-items: start;
  justify-content: space-between;
  gap: 12px;
  padding: 20px;
  border-bottom: 1px solid rgba(255,255,255,0.08);
}
.modal-title {
  font-size: 14px;
  font-weight: 700;
  color: #c9d1d9;
  margin-bottom: 4px;
}
.modal-subtitle {
  font-size: 11px;
  letter-spacing: 2px;
  text-transform: uppercase;
  color: #848a94;
}
.modal-close {
  font-family: inherit;
  font-size: 18px;
  background: none;
  border: none;
  color: #6b7280;
  cursor: pointer;
  padding: 4px 8px;
  flex-shrink: 0;
  line-height: 1;
}
.modal-close:hover { color: #fff; }
.modal-body {
  flex: 1;
  overflow-y: auto;
  padding: 20px;
  font-size: var(--text-base);
  color: #8b949e;
  line-height: 1.8;
}
.modal-section-title {
  font-size: var(--text-sm);
  letter-spacing: 0.15em;
  text-transform: uppercase;
  color: var(--cyan-dim);
  margin-bottom: 12px;
  font-weight: 400;
}
.modal-jp-text {
  font-size: var(--text-base);
  line-height: 2;
  color: #c9d1d9;
  white-space: pre-wrap;
  padding: 16px;
  background: rgba(255,255,255,0.03);
  border-radius: 4px;
  border-left: 2px solid var(--cyan-dim);
}
.modal-en-text {
  font-size: var(--text-base);
  line-height: 1.8;
  color: #b0b8c4;
  white-space: pre-wrap;
}
.modal-en-text strong {
  color: var(--cyan);
}
.modal-attribution {
  margin-top: 24px;
  padding-top: 16px;
  border-top: 1px solid rgba(255,255,255,0.06);
  font-size: var(--text-xs);
}
.modal-attribution a {
  color: var(--cyan-dim);
  text-decoration: none;
}
.modal-attribution a:hover {
  text-decoration: underline;
}

/* Modal vocab / translation grid */
.modal-vocab {
  margin-bottom: 20px;
}
.vocab-item {
  padding: 6px 0;
  border-bottom: 1px solid rgba(255,255,255,0.04);
  font-size: var(--text-sm);
}
.vocab-item strong {
  color: var(--orange-dim);
}
.modal-translation-grid {
  margin: 16px 0;
}
.translation-row {
  display: flex;
  gap: 12px;
  padding: 8px 12px;
  border-bottom: 1px solid rgba(255,255,255,0.04);
  align-items: center;
}
.tl-jp {
  font-size: 1.1rem;
  color: var(--text-primary);
  min-width: 60px;
}
.tl-action {
  color: var(--cyan);
  font-weight: 600;
  min-width: 120px;
}
.tl-target {
  color: var(--text-dim);
}
.modal-mnemonic {
  margin-top: 20px;
  padding: 16px;
  background: rgba(255,255,255,0.03);
  border-radius: 4px;
  border-left: 2px solid var(--orange-dim);
}
.mnemonic-list {
  margin-top: 8px;
  font-size: 1rem;
  color: var(--text-primary);
  line-height: 1.8;
  font-weight: 600;
  letter-spacing: 0.05em;
}

/* ─── Four Dimensions (Act 2) ─── */
.four-dimensions {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 12px;
  margin: 32px 0;
}
.dimension-card {
  padding: 20px;
  border: 1px solid var(--border);
  border-radius: 6px;
  background: var(--surface);
  position: relative;
}
.dimension-card h3 {
  font-size: var(--text-base);
  font-weight: 600;
  color: var(--text-primary);
  margin-bottom: 4px;
}
.dimension-jp {
  font-size: var(--text-sm);
  color: var(--cyan-dim);
  margin-bottom: 8px;
  letter-spacing: 0.1em;
}
.dimension-number {
  position: absolute;
  top: 12px;
  right: 14px;
  font-size: 1.5rem;
  font-weight: 700;
  color: rgba(0,212,255,0.12);
  line-height: 1;
}
.dimension-card p {
  font-size: var(--text-sm);
  color: var(--text-dim);
  line-height: 1.7;
  margin: 0;
}

/* ─── Build Yourself Inline Viz (Act 2) ─── */
.build-yourself-inline {
  margin: 24px -24px;
  padding: 12px 8px;
  width: calc(100% + 48px);
  background: #050509;
  border-top: 1px solid var(--border);
  border-bottom: 1px solid var(--border);
}
.by-controls {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 0 8px;
  margin-bottom: 4px;
}
.by-play-btn {
  font-family: inherit;
  font-size: 15px;
  padding: 2px 8px;
  border-radius: 3px;
  cursor: pointer;
  border: 1px solid #21262d;
  background: transparent;
  color: #6b7280;
  transition: all 0.15s;
  line-height: 1;
  flex-shrink: 0;
}
.by-play-btn:hover { border-color: #00d4ff; color: #00d4ff; }
.by-label-end {
  font-size: 8px;
  letter-spacing: 0.14em;
  color: #6b7280;
  text-transform: uppercase;
  flex-shrink: 0;
}
.by-slider {
  flex: 1;
  height: 4px;
  cursor: pointer;
  accent-color: #00d4ff;
}
.by-slider-label {
  font-size: var(--text-sm);
  color: #6b7280;
  text-align: center;
  letter-spacing: 0.08em;
  min-height: 18px;
  padding: 0 8px;
}
.by-steps {
  display: flex;
  justify-content: center;
  gap: 4px;
  flex-wrap: wrap;
  padding: 4px 8px 8px;
}
.by-step-btn {
  font-family: 'Courier New', Consolas, Monaco, monospace;
  font-size: clamp(9px, 1.4vw, 12px);
  letter-spacing: 0.1em;
  text-transform: uppercase;
  padding: 3px 10px;
  border-radius: 2px;
  cursor: pointer;
  border: 1px solid #21262d;
  background: transparent;
  color: #6b7280;
  transition: all 0.15s;
  white-space: nowrap;
}
.by-step-btn:hover { border-color: #888; color: #ccc; }
.by-step-btn.lit { box-shadow: 0 0 6px rgba(0,0,0,0.3); }
.by-step-btn.active { box-shadow: 0 0 8px rgba(255,255,255,0.2); }
.by-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  grid-template-rows: 1fr 1fr;
  gap: 2px;
  background: #21262d;
  border-radius: 6px;
  overflow: hidden;
  width: 100%;
  aspect-ratio: 1 / 1;
  max-width: 800px;
  margin: 0 auto;
}
.by-panel {
  position: relative;
  overflow: hidden;
  background: #0a0e14;
  aspect-ratio: 1 / 1;
}
.by-panel canvas {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
}

/* ─── Viz Containers ─── */
.viz-container {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: 6px;
  padding: 16px 12px;
  min-height: 200px;
  overflow: auto;
  -webkit-overflow-scrolling: touch;
  touch-action: pan-x pan-y pinch-zoom;
}

.disclaimer {
  margin-top: 24px;
  padding: 12px 16px;
  font-size: var(--text-xs);
  color: var(--text-dim);
  border-left: 2px solid var(--border);
  line-height: 1.6;
}

.viz-zoomable {
  position: relative;
  overflow: hidden;
  touch-action: none;
}
.viz-zoomable .viz-container {
  transform-origin: 0 0;
  overflow: visible;
}
.zoom-hint {
  display: none;
  text-align: center;
  font-size: var(--text-xs);
  color: var(--text-dim);
  margin-top: 6px;
  opacity: 0.6;
}
@media (max-width: 600px) {
  .zoom-hint { display: block; }
}

/* ─── Examples Box (Act 5) ─── */
.examples-box {
  margin-top: 32px;
  border: 1px solid var(--border);
  border-radius: 6px;
  background: var(--surface);
  padding: 20px;
}
.examples-box h3 {
  font-size: var(--text-sm);
  letter-spacing: 0.15em;
  text-transform: uppercase;
  color: var(--text-dim);
  margin-bottom: 16px;
}
.example-item {
  padding: 12px 0;
  border-bottom: 1px solid rgba(255,255,255,0.04);
  font-size: var(--text-sm);
  color: var(--text-muted);
  line-height: 1.7;
}
.example-item:last-child { border-bottom: none; }
.example-label {
  display: block;
  color: var(--cyan-dim);
  font-weight: 600;
  font-size: 0.8rem;
  letter-spacing: 0.08em;
  margin-bottom: 4px;
}

/* ─── CTA / Act 6 ─── */
.cta-layout {
  display: flex;
  flex-direction: column;
  gap: 0;
}
.cta-center {
  position: relative;
  width: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 24px 0;
}
.cta-left {
  position: absolute;
  left: 0;
  width: 45%;
  text-align: left;
  z-index: 1;
}
.cta-title {
  font-size: clamp(1.6rem, 4vw, 2.6rem);
  font-weight: 400;
  color: var(--text-primary);
  letter-spacing: 0.15em;
  text-transform: uppercase;
  line-height: 1.4;
  margin: 0 0 16px 0;
}
.cta-lead {
  font-size: clamp(0.85rem, 2vw, 1rem);
  color: var(--text-secondary);
  line-height: 1.7;
  max-width: 320px;
}
.cta-calligraphy {
  width: clamp(260px, 45vw, 480px);
  height: auto;
  opacity: 0.8;
  pointer-events: none;
  user-select: none;
}
.cta-bottom-right {
  align-self: flex-end;
  text-align: right;
  max-width: 520px;
  font-size: clamp(0.85rem, 2vw, 1rem);
  color: var(--text-secondary);
  line-height: 1.7;
  margin-top: 8px;
}

/* ─── Inline Links ─── */
.inline-link {
  color: var(--cyan);
  text-decoration: none;
  border-bottom: 1px solid rgba(0,212,255,0.3);
  transition: all 0.2s;
}
.inline-link:hover {
  text-shadow: var(--glow-cyan);
  border-bottom-color: var(--cyan);
}

/* ─── Mobile ─── */
@media (max-width: 600px) {
  .four-dimensions { grid-template-columns: 1fr; }
  .source-buttons { flex-direction: column; }
  .act { padding: 60px 16px; }
  .contrast-callout { flex-direction: column; gap: 8px; }
  .translation-row { flex-direction: column; gap: 4px; }
  .hero-center { flex-direction: column; }
  .hero-subtitle {
    position: relative;
    left: auto;
    width: 100%;
    margin-bottom: 16px;
  }
  .cta-center { flex-direction: column; }
  .cta-left {
    position: relative;
    left: auto;
    width: 100%;
    text-align: center;
    margin-bottom: 16px;
  }
  .cta-lead { max-width: none; }
  .cta-bottom-right {
    align-self: center;
    text-align: center;
    max-width: none;
  }
}
</style>

<!-- ═══════════════════════════════════════════════════════════════════════════ -->
<!-- SCRIPTS                                                                   -->
<!-- ═══════════════════════════════════════════════════════════════════════════ -->
<script is:inline>

// ═══ Scroll Reveal + Deep Link Hash Tracking ═══
(function() {
  const acts = document.querySelectorAll('.act');
  let currentAct = null;

  const observer = new IntersectionObserver((entries) => {
    entries.forEach((e) => {
      if (e.isIntersecting) {
        e.target.classList.add('visible');
        const actId = e.target.id;
        if (actId && actId !== currentAct) {
          currentAct = actId;
          history.replaceState(null, '', location.pathname + location.search + '#' + actId);
        }
      }
    });
  }, { threshold: 0.15 });
  acts.forEach((a) => observer.observe(a));

  const hash = location.hash.replace('#', '');
  if (hash) {
    const target = document.getElementById(hash);
    if (target) {
      setTimeout(() => {
        target.scrollIntoView({ behavior: 'smooth' });
        target.classList.add('visible');
      }, 200);
    }
  }
})();

// ═══ Deep Link Button ═══
(function() {
  const btn = document.getElementById('deeplink-btn');
  if (!btn) return;
  btn.addEventListener('click', () => {
    navigator.clipboard.writeText(location.href).then(() => {
      btn.textContent = 'Copied!';
      btn.style.color = '#00d4ff';
      btn.style.borderColor = 'rgba(0,212,255,0.4)';
      setTimeout(() => {
        btn.textContent = 'Link';
        btn.style.color = '';
        btn.style.borderColor = '';
      }, 1500);
    });
  });
})();

// ═══ Language Toggle ═══
(function() {
  const btn = document.getElementById('lang-toggle');
  if (!btn) return;
  const labels = btn.querySelectorAll('.lang-label');
  let lang = 'en';
  btn.addEventListener('click', () => {
    lang = lang === 'en' ? 'ja' : 'en';
    document.body.classList.toggle('lang-ja', lang === 'ja');
    labels.forEach((l) => {
      if ((lang === 'en' && l.textContent === 'EN') || (lang === 'ja' && l.textContent === 'JP')) {
        l.setAttribute('data-active', '');
      } else {
        l.removeAttribute('data-active');
      }
    });
  });
})();

// ═══ Modal System ═══
(function() {
  function openModal(id) {
    const m = document.getElementById(id);
    if (m) m.classList.add('active');
  }
  function closeModal(m) {
    m.classList.remove('active');
  }

  // Open buttons
  const btnKodokan = document.getElementById('btn-kodokan');
  const btnKuzushi = document.getElementById('btn-kuzushi-xyz');
  if (btnKodokan) btnKodokan.addEventListener('click', () => openModal('modal-kodokan'));
  if (btnKuzushi) btnKuzushi.addEventListener('click', () => openModal('modal-kuzushi-xyz'));

  // Close buttons
  document.querySelectorAll('.modal-close').forEach(btn => {
    btn.addEventListener('click', () => {
      const overlay = btn.closest('.modal-overlay');
      if (overlay) closeModal(overlay);
    });
  });

  // Click outside
  document.querySelectorAll('.modal-overlay').forEach(overlay => {
    overlay.addEventListener('click', (e) => {
      if (e.target === overlay) closeModal(overlay);
    });
  });

  // Escape key
  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape') {
      document.querySelectorAll('.modal-overlay.active').forEach(closeModal);
    }
  });
})();

// ═══════════════════════════════════════════════════════════════════════════════
// f(Tsukuri) / f(Kuzushi) Visualization Factory
// Ported from FunctionOfEverything — SVG radial node graph with time slider
// ═══════════════════════════════════════════════════════════════════════════════

const FUNCTION_DATA = {
  Tsukuri: {
    title: "Tsukuri",
    titleJa: "作り",
    subtitle: "The Accumulation of Advantage",
    subtitleJa: "優位の蓄積",
    description: "In Tsukuri, Time (t) is your ally. It accumulates.",
    descriptionJa: "作りにおいて、時間は味方。蓄積する。",
    timeEffect: "grow",
    components: [
      { id: "space", name: "Spatial Positioning", nameJa: "空間的位置取り", desc: "Closing distance, securing the angle, penetrating space.", sub: [
        { id: "spa_dist", name: "Distance", nameJa: "距離" },
        { id: "spa_angle", name: "Angle", nameJa: "角度" },
        { id: "spa_level", name: "Level Change", nameJa: "レベル変化" }
      ]},
      { id: "align", name: "Structural Alignment", nameJa: "構造的整列", desc: "Stacking bones, engaging the core, Jibun wo Tsukuru.", sub: [
        { id: "ali_stack", name: "Bone Stacking", nameJa: "骨の積み重ね" },
        { id: "ali_core", name: "Core Engage", nameJa: "体幹の活性化" },
        { id: "ali_post", name: "Posture", nameJa: "姿勢" }
      ]},
      { id: "anchor", name: "Connection", nameJa: "接続", desc: "Establishing friction, locking limbs to center of mass.", sub: [
        { id: "anc_grip", name: "Grip", nameJa: "グリップ" },
        { id: "anc_hook", name: "Hooks", nameJa: "フック" },
        { id: "anc_press", name: "Pressure", nameJa: "プレッシャー" }
      ]},
      { id: "load", name: "Kinetic Loading", nameJa: "運動エネルギー蓄積", desc: "Dropping weight, coiling hips, storing potential energy.", sub: [
        { id: "lod_drop", name: "Weight Drop", nameJa: "体重落とし" },
        { id: "lod_coil", name: "Hip Coil", nameJa: "腰の巻き" },
        { id: "lod_store", name: "Stored Energy", nameJa: "蓄積エネルギー" }
      ]},
      { id: "wedge", name: "Wedge Insertion", nameJa: "楔の挿入", desc: "Placing knees/elbows to block recovery or create fulcrums.", sub: [
        { id: "wed_knee", name: "Knee Wedge", nameJa: "膝の楔" },
        { id: "wed_elbow", name: "Elbow Frame", nameJa: "肘のフレーム" },
        { id: "wed_hip", name: "Hip Block", nameJa: "腰のブロック" }
      ]}
    ],
    theme: { main: "#16a34a", glow: "#86efac", sub: "#22c55e", bgDark: "#052e16", strokeDark: "#166534", bgNode: "#031c0a" }
  },
  Kuzushi: {
    title: "Kuzushi",
    titleJa: "崩し",
    subtitle: "The Window of Vulnerability",
    subtitleJa: "脆弱性の窓",
    description: "In Kuzushi, Time (t) is the enemy. It decays.",
    descriptionJa: "崩しにおいて、時間は敵。減衰する。",
    timeEffect: "decay",
    components: [
      { id: "misalign", name: "Structural Misalignment", nameJa: "構造的不整列", desc: "Breaking the spinal line, folding frames, center of gravity shift.", sub: [
        { id: "mis_spine", name: "Spinal Break", nameJa: "脊椎の折れ" },
        { id: "mis_fold", name: "Frame Fold", nameJa: "フレームの崩壊" },
        { id: "mis_cog", name: "CoG Shift", nameJa: "重心移動" }
      ]},
      { id: "base", name: "Base Removal", nameJa: "ベースの除去", desc: "Taking away a post, sweeping a foot, elevating hips.", sub: [
        { id: "bas_post", name: "Post Removal", nameJa: "支柱の除去" },
        { id: "bas_sweep", name: "Nose Over Toes", nameJa: "重心の逸脱" },
        { id: "bas_elev", name: "Hip Elevation", nameJa: "腰の挙上" }
      ]},
      { id: "moment", name: "Momentum", nameJa: "運動量", desc: "Redirecting force, using kinetic energy against the source.", sub: [
        { id: "mom_redir", name: "Redirection", nameJa: "方向転換" },
        { id: "mom_chain", name: "Action-Reaction", nameJa: "作用・反作用" },
        { id: "mom_inert", name: "Inertia", nameJa: "慣性" }
      ]},
      { id: "cog", name: "Cognitive Disruption", nameJa: "認知的撹乱", desc: "Attention misdirection, sensory overload, lag.", sub: [
        { id: "cog_feint", name: "Feint", nameJa: "フェイント" },
        { id: "cog_over", name: "Overload", nameJa: "過負荷" },
        { id: "cog_tempo", name: "Tempo Break", nameJa: "テンポ崩し" }
      ]},
      { id: "iso", name: "Isolation", nameJa: "孤立化", desc: "Separating limbs from the core, denying connection.", sub: [
        { id: "iso_limb", name: "Limb Separation", nameJa: "四肢の分離" },
        { id: "iso_pin", name: "Pinning", nameJa: "ピニング" },
        { id: "iso_deny", name: "Connection Denial", nameJa: "接続の遮断" }
      ]}
    ],
    theme: { main: "#ea580c", glow: "#fdba74", sub: "#f97316", bgDark: "#431407", strokeDark: "#9a3412", bgNode: "#2c0b02" }
  }
};

function initFunctionViz(containerId, dataKey, prefix) {
  const container = document.getElementById(containerId);
  if (!container) return;

  const vizData = FUNCTION_DATA[dataKey];
  const theme = vizData.theme;
  const isJa = () => document.body.classList.contains('lang-ja');

  // Component values (all start at 50)
  const componentValues = {};
  vizData.components.forEach(c => { componentValues[c.id] = 50; });
  let timeValue = 50;
  let hoveredNodeId = null;

  // SVG dimensions
  const svgW = 700, svgH = 500;
  const cx = svgW / 2, cy = svgH / 2;

  // Build DOM
  container.innerHTML = '';

  // Title
  const titleDiv = document.createElement('div');
  titleDiv.style.cssText = 'text-align:center;margin-bottom:16px;';
  titleDiv.innerHTML = `
    <div style="font-size:1.2rem;font-weight:600;color:${theme.main};letter-spacing:0.05em;">
      <span lang="en">ƒ(${vizData.title})</span><span lang="ja">ƒ(${vizData.titleJa})</span>
    </div>
    <div style="font-size:0.8rem;color:${theme.glow};letter-spacing:0.1em;">
      <span lang="en">${vizData.subtitle}</span><span lang="ja">${vizData.subtitleJa}</span>
    </div>`;
  container.appendChild(titleDiv);

  // SVG
  const svgNs = 'http://www.w3.org/2000/svg';
  const svg = document.createElementNS(svgNs, 'svg');
  svg.setAttribute('viewBox', `0 0 ${svgW} ${svgH}`);
  svg.setAttribute('width', '100%');
  svg.style.cssText = 'display:block;width:100%;margin:0 auto;';

  // Glow filter
  const defs = document.createElementNS(svgNs, 'defs');
  defs.innerHTML = `<filter id="${prefix}-glow" x="-50%" y="-50%" width="200%" height="200%">
    <feGaussianBlur in="SourceGraphic" stdDeviation="4" result="blur"/>
    <feMerge><feMergeNode in="blur"/><feMergeNode in="SourceGraphic"/></feMerge>
  </filter>`;
  svg.appendChild(defs);

  const linesGroup = document.createElementNS(svgNs, 'g');
  const nodesGroup = document.createElementNS(svgNs, 'g');
  svg.appendChild(linesGroup);
  svg.appendChild(nodesGroup);

  // Center core
  const centerCore = document.createElementNS(svgNs, 'circle');
  centerCore.setAttribute('cx', cx);
  centerCore.setAttribute('cy', cy);
  centerCore.setAttribute('r', 40);
  centerCore.setAttribute('fill', theme.bgDark);
  centerCore.setAttribute('stroke', theme.main);
  centerCore.setAttribute('stroke-width', '2');
  svg.appendChild(centerCore);

  const centerText = document.createElementNS(svgNs, 'text');
  centerText.setAttribute('x', cx);
  centerText.setAttribute('y', cy + 5);
  centerText.setAttribute('text-anchor', 'middle');
  centerText.setAttribute('fill', theme.glow);
  centerText.setAttribute('font-size', '14px');
  centerText.setAttribute('font-weight', 'bold');
  centerText.textContent = `ƒ(${vizData.title})`;
  svg.appendChild(centerText);

  container.appendChild(svg);

  // Tooltip
  const tooltip = document.createElement('div');
  tooltip.style.cssText = `min-height:40px;margin-top:8px;text-align:center;font-size:0.8rem;color:${theme.glow};opacity:0;transition:opacity 0.2s;`;
  container.appendChild(tooltip);



  function createSvgEl(tag) {
    return document.createElementNS(svgNs, tag);
  }

  function buildSvgElements() {
    linesGroup.innerHTML = '';
    nodesGroup.innerHTML = '';
    const numNodes = vizData.components.length;

    vizData.components.forEach((comp, i) => {
      const angle = (Math.PI * 2 * i) / numNodes - Math.PI / 2;
      const nodeX = cx + Math.cos(angle) * 150;
      const nodeY = cy + Math.sin(angle) * 150;

      // Main line
      const line = createSvgEl('line');
      line.setAttribute('x1', cx);
      line.setAttribute('y1', cy);
      line.setAttribute('x2', nodeX);
      line.setAttribute('y2', nodeY);
      line.setAttribute('id', `${prefix}-line-${comp.id}`);
      linesGroup.appendChild(line);

      // Sub-nodes
      if (comp.sub) {
        const numSub = comp.sub.length;
        const wedgeAngle = (Math.PI * 2) / numNodes;
        const wedgeStart = angle - (wedgeAngle / 2);
        const padding = wedgeAngle * 0.1;
        const usableStart = wedgeStart + padding;
        const usableAngle = wedgeAngle - (padding * 2);

        comp.sub.forEach((subComp, j) => {
          const subAngle = numSub > 1
            ? usableStart + (usableAngle * (j / (numSub - 1)))
            : angle;
          const outerR = 230;
          const subX = cx + Math.cos(subAngle) * outerR;
          const subY = cy + Math.sin(subAngle) * outerR;

          // Sub-line
          const subLine = createSvgEl('line');
          subLine.setAttribute('x1', nodeX);
          subLine.setAttribute('y1', nodeY);
          subLine.setAttribute('x2', subX);
          subLine.setAttribute('y2', subY);
          subLine.setAttribute('id', `${prefix}-line-${subComp.id}`);
          subLine.setAttribute('stroke', theme.strokeDark);
          subLine.setAttribute('stroke-dasharray', '2,2');
          linesGroup.appendChild(subLine);

          // Sub-circle
          const subCircle = createSvgEl('circle');
          subCircle.setAttribute('cx', subX);
          subCircle.setAttribute('cy', subY);
          subCircle.setAttribute('id', `${prefix}-circle-${subComp.id}`);
          subCircle.setAttribute('r', '5');
          subCircle.setAttribute('fill', theme.bgNode);
          subCircle.setAttribute('stroke', theme.glow);
          subCircle.setAttribute('stroke-width', '1');
          nodesGroup.appendChild(subCircle);

          // Sub-text
          const subText = createSvgEl('text');
          subText.setAttribute('x', subX);
          subText.setAttribute('y', subY);
          subText.setAttribute('id', `${prefix}-text-${subComp.id}`);
          subText.setAttribute('text-anchor', 'middle');
          subText.setAttribute('font-size', '10px');
          subText.setAttribute('fill', '#ccc');
          subText.textContent = isJa() ? (subComp.nameJa || subComp.name) : subComp.name;
          nodesGroup.appendChild(subText);
        });
      }

      // Node group (interactive)
      const g = createSvgEl('g');
      g.style.cursor = 'pointer';
      g.addEventListener('mouseenter', () => setHover(comp.id));
      g.addEventListener('mouseleave', () => setHover(null));
      g.addEventListener('touchstart', (e) => { e.preventDefault(); setHover(hoveredNodeId === comp.id ? null : comp.id); }, { passive: false });

      const circle = createSvgEl('circle');
      circle.setAttribute('cx', nodeX);
      circle.setAttribute('cy', nodeY);
      circle.setAttribute('id', `${prefix}-circle-${comp.id}`);
      circle.setAttribute('stroke-width', '2');

      const text = createSvgEl('text');
      text.setAttribute('x', nodeX);
      text.setAttribute('id', `${prefix}-text-${comp.id}`);
      text.setAttribute('text-anchor', 'middle');
      text.textContent = isJa() ? (comp.nameJa || comp.name) : comp.name;

      g.appendChild(circle);
      g.appendChild(text);
      nodesGroup.appendChild(g);
    });
  }

  function setHover(id) {
    hoveredNodeId = id;
    if (id) {
      const comp = vizData.components.find(c => c.id === id);
      if (comp) {
        tooltip.style.opacity = '1';
        tooltip.innerHTML = `<strong>${isJa() ? (comp.nameJa || comp.name) : comp.name}</strong><br/><span style="color:#9CA3AF;font-size:0.75rem;">${comp.desc}</span>`;
      }
    } else {
      tooltip.style.opacity = '0';
    }
    updateVisualization();
  }

  function updateVisualization() {
    const numNodes = vizData.components.length;
    const vals = Object.values(componentValues);
    let baseOutput = vals.reduce((a, b) => a + b, 0) / vals.length;
    let output = 0;

    if (vizData.timeEffect === 'decay') {
      output = baseOutput * Math.max(0, 100 - timeValue) / 100;
    } else {
      output = Math.min(100, baseOutput * (0.5 + (timeValue / 100)));
    }

    // Update center core
    const coreR = Math.min(80, Math.max(20, 30 + (output / 2.5)));
    centerCore.setAttribute('r', coreR);
    centerCore.setAttribute('stroke', output > 80 ? theme.glow : theme.main);
    centerCore.setAttribute('stroke-width', output > 80 ? '4' : '2');
    centerCore.setAttribute('filter', output > 70 ? `url(#${prefix}-glow)` : 'none');
    centerText.textContent = `ƒ(${isJa() ? vizData.titleJa : vizData.title})`;


    vizData.components.forEach((comp, i) => {
      const angle = (Math.PI * 2 * i) / numNodes - Math.PI / 2;
      const isHovered = (hoveredNodeId === comp.id);
      const val = componentValues[comp.id] || 0;
      const nodeSize = 25 + (val / 4);
      const currentRadius = coreR + nodeSize + 15;
      const nodeX = cx + Math.cos(angle) * currentRadius;
      const nodeY = cy + Math.sin(angle) * currentRadius;

      const line = document.getElementById(`${prefix}-line-${comp.id}`);
      if (line) {
        line.setAttribute('x2', nodeX);
        line.setAttribute('y2', nodeY);
        line.setAttribute('stroke', isHovered ? theme.main : theme.strokeDark);
        line.setAttribute('stroke-width', isHovered ? '3' : String(1 + val / 30));
        line.setAttribute('stroke-dasharray', isHovered ? 'none' : '4,4');
      }

      const circle = document.getElementById(`${prefix}-circle-${comp.id}`);
      if (circle) {
        circle.setAttribute('cx', nodeX);
        circle.setAttribute('cy', nodeY);
        circle.setAttribute('r', nodeSize);
        circle.setAttribute('fill', isHovered ? theme.main : theme.bgNode);
        circle.setAttribute('stroke', isHovered ? theme.glow : theme.strokeDark);
        circle.setAttribute('filter', (isHovered || val > 70) ? `url(#${prefix}-glow)` : 'none');
      }

      const text = document.getElementById(`${prefix}-text-${comp.id}`);
      if (text) {
        text.setAttribute('x', nodeX);
        text.setAttribute('y', nodeY);
        text.setAttribute('fill', isHovered ? '#fff' : '#e5e0d8');
        text.setAttribute('font-size', isHovered ? '11px' : '9px');
        text.setAttribute('font-weight', isHovered ? 'bold' : 'normal');
        text.setAttribute('dominant-baseline', 'central');
        text.textContent = isJa() ? (comp.nameJa || comp.name) : comp.name;
      }

      // Sub-nodes
      if (comp.sub) {
        const numSub = comp.sub.length;
        const wedgeAngle = (Math.PI * 2) / numNodes;
        const wedgeStart = angle - (wedgeAngle / 2);
        const padding = wedgeAngle * 0.1;
        const usableStart = wedgeStart + padding;
        const usableAngle = wedgeAngle - (padding * 2);
        const outerR = 200 + (val / 4);

        comp.sub.forEach((sub, j) => {
          const subAngle = numSub > 1
            ? usableStart + (usableAngle * (j / (numSub - 1)))
            : angle;
          const subX = cx + Math.cos(subAngle) * outerR;
          const subY = cy + Math.sin(subAngle) * outerR;
          const subSize = 4 + (val / 25);

          const subCircle = document.getElementById(`${prefix}-circle-${sub.id}`);
          if (subCircle) {
            subCircle.setAttribute('cx', subX);
            subCircle.setAttribute('cy', subY);
            subCircle.setAttribute('r', subSize);
            subCircle.setAttribute('stroke', isHovered ? theme.glow : theme.strokeDark);
            subCircle.setAttribute('fill', isHovered ? theme.main : theme.bgNode);
          }
          const subText = document.getElementById(`${prefix}-text-${sub.id}`);
          if (subText) {
            subText.setAttribute('fill', isHovered ? '#fff' : '#ccc');
            subText.setAttribute('font-size', '10px');
            const dx = subX - cx;
            const dy = subY - cy;
            const labelR = 14 + subSize;
            const labelX = subX + (dx / outerR) * labelR;
            const labelY = subY + (dy / outerR) * labelR;
            let anchor = 'middle';
            if (dx > 30) anchor = 'start';
            else if (dx < -30) anchor = 'end';
            subText.setAttribute('x', labelX);
            subText.setAttribute('y', labelY);
            subText.setAttribute('text-anchor', anchor);
            subText.setAttribute('dominant-baseline', 'central');
            subText.textContent = isJa() ? (sub.nameJa || sub.name) : sub.name;
          }
          const subLine = document.getElementById(`${prefix}-line-${sub.id}`);
          if (subLine) {
            subLine.setAttribute('x1', nodeX);
            subLine.setAttribute('y1', nodeY);
            subLine.setAttribute('x2', subX);
            subLine.setAttribute('y2', subY);
            subLine.setAttribute('stroke', isHovered ? theme.main : theme.strokeDark);
          }
        });
      }
    });
  }

  // Re-render on language change
  const langBtn = document.getElementById('lang-toggle');
  if (langBtn) langBtn.addEventListener('click', () => {
    setTimeout(() => { buildSvgElements(); updateVisualization(); }, 10);
  });

  buildSvgElements();
  updateVisualization();
}

// ═══════════════════════════════════════════════════════════════════════════════
// Act 5 — Flow Diagram: Tsukuri → Kuzushi → Kake
// ═══════════════════════════════════════════════════════════════════════════════

function initFlowViz(containerId) {
  const container = document.getElementById(containerId);
  if (!container) return;
  const isJa = () => document.body.classList.contains('lang-ja');

  function render() {
    const ja = isJa();
    container.innerHTML = '';

    // Main flow: [ Tsukuri frame [ Jibun wo Tsukuru + Kuzushi ] ] → Kake
    const flow = document.createElement('div');
    flow.style.cssText = 'display:flex;align-items:center;justify-content:center;gap:0;flex-wrap:wrap;';

    // === Tsukuri outer frame ===
    const tsukuriFrame = document.createElement('div');
    tsukuriFrame.style.cssText = 'border:2px solid #4C8DFF44;border-radius:8px;padding:12px;background:rgba(76,141,255,0.03);position:relative;';

    // Tsukuri label
    const tsukuriLabel = document.createElement('div');
    tsukuriLabel.style.cssText = 'position:absolute;top:-12px;left:50%;transform:translateX(-50%);background:#0E1014;padding:0 10px;font-size:0.8rem;font-weight:700;color:#4C8DFF;letter-spacing:0.1em;white-space:nowrap;';
    tsukuriLabel.textContent = ja ? '作り / Tsukuri' : 'Tsukuri / 作り';
    tsukuriFrame.appendChild(tsukuriLabel);

    // Inner boxes container
    const inner = document.createElement('div');
    inner.style.cssText = 'display:flex;align-items:stretch;gap:12px;margin-top:8px;';

    // Jibun wo Tsukuru box (green)
    const jibunBox = document.createElement('div');
    jibunBox.style.cssText = 'flex:1;min-width:150px;padding:16px 14px;border:1px solid #16a34a44;border-radius:6px;background:#052e16;text-align:center;';
    jibunBox.innerHTML = `
      <div style="font-size:1rem;font-weight:700;color:#16a34a;margin-bottom:4px;">${ja ? '自分を作る' : 'Jibun wo Tsukuru'}</div>
      <div style="font-size:0.7rem;color:#86efac;letter-spacing:0.1em;margin-bottom:8px;text-transform:uppercase;">${ja ? '自分を構築する' : 'Build Yourself'}</div>
      <div style="font-size:0.7rem;color:#9CA3AF;line-height:1.5;">${ja ? '姿勢、グリップ、構造、整列' : 'Posture, grips, structure, alignment'}</div>`;
    inner.appendChild(jibunBox);

    // + sign
    const plus = document.createElement('div');
    plus.style.cssText = 'display:flex;align-items:center;font-size:1.2rem;color:#6B7280;flex-shrink:0;';
    plus.textContent = '+';
    inner.appendChild(plus);

    // Kuzushi box (orange)
    const kuzushiBox = document.createElement('div');
    kuzushiBox.style.cssText = 'flex:1;min-width:150px;padding:16px 14px;border:1px solid #ea580c44;border-radius:6px;background:#431407;text-align:center;';
    kuzushiBox.innerHTML = `
      <div style="font-size:1rem;font-weight:700;color:#ea580c;margin-bottom:4px;">${ja ? '崩し' : 'Kuzushi'}</div>
      <div style="font-size:0.7rem;color:#fdba74;letter-spacing:0.1em;margin-bottom:8px;text-transform:uppercase;">${ja ? '崩壊' : 'Break / Disrupt'}</div>
      <div style="font-size:0.7rem;color:#9CA3AF;line-height:1.5;">${ja ? '不整列、ベース除去、孤立化' : 'Misalign, remove base, isolate'}</div>`;
    inner.appendChild(kuzushiBox);

    tsukuriFrame.appendChild(inner);
    flow.appendChild(tsukuriFrame);

    // Arrow → Kake
    const arrow = document.createElement('div');
    arrow.style.cssText = 'display:flex;align-items:center;padding:0 12px;font-size:1.4rem;color:#6B7280;flex-shrink:0;';
    arrow.textContent = '→';
    flow.appendChild(arrow);

    // Kake box (red)
    const kakeBox = document.createElement('div');
    kakeBox.style.cssText = 'min-width:140px;max-width:180px;padding:20px 16px;border:1px solid #dc262644;border-radius:6px;background:#450a0a;text-align:center;align-self:stretch;display:flex;flex-direction:column;justify-content:center;';
    kakeBox.innerHTML = `
      <div style="font-size:1.1rem;font-weight:700;color:#dc2626;margin-bottom:4px;">${ja ? '掛け' : 'Kake'}</div>
      <div style="font-size:0.7rem;color:#fca5a5;letter-spacing:0.1em;margin-bottom:8px;text-transform:uppercase;">${ja ? '実行' : 'Execute'}</div>
      <div style="font-size:0.7rem;color:#9CA3AF;line-height:1.5;">${ja ? '条件が揃った時に技を実行' : 'Perform the technique when conditions are met'}</div>`;
    flow.appendChild(kakeBox);

    container.appendChild(flow);

    // Insight text
    const insight = document.createElement('div');
    insight.style.cssText = 'text-align:center;margin-top:16px;font-size:0.8rem;color:#9CA3AF;line-height:1.7;max-width:600px;margin-left:auto;margin-right:auto;';
    insight.innerHTML = ja
      ? '最初の二つは入れ替え可能というよりも、どちらも前提条件である。相手を常に崩しの状態に置くことはできないが、自分の身体を常にレジリエントな状態に保つことはコントロールできるはずだ。「ベース」はこの概念に関連する用語で、<span style="color:#00d4ff;">「力を吸収し、力を生み出す能力」</span>と表現されることがある。'
      : 'It\'s not really that the first two are interchangeable, more like they\'re both pre-requisites. While we cannot always have the other in a state of Kuzushi, we should be able to control our body to always be in a resilient state. "Base" is another term associated with this idea, I heard it described by Canadians as <span style="color:#00d4ff;">"the ability to absorb and generate force"</span>. Great definition.';
    container.appendChild(insight);
  }

  render();

  const langBtn = document.getElementById('lang-toggle');
  if (langBtn) langBtn.addEventListener('click', () => setTimeout(render, 10));
}

// ═══ Initialize All Visualizations ═══
initFunctionViz('viz-tsukuri', 'Tsukuri', 'tsu');
initFunctionViz('viz-kuzushi', 'Kuzushi', 'kuz');
initFlowViz('viz-flow');

// ═══ Build Yourself Inline Viz ═══
function initBuildYourselfViz() {
// ============================================================
// 1. Math utils
// ============================================================
function lerp(a, b, t) { return a + (b - a) * t; }
function clamp(v, min, max) {
  if (min === undefined) { min = 0; max = 1; }
  return Math.max(min, Math.min(max, v));
}
function easeInOut(t) { return t * t * (3 - 2 * t); }
function dist(a, b) {
  var dx = b.x - a.x, dy = b.y - a.y;
  return Math.sqrt(dx * dx + dy * dy);
}
function lerpPos(a, b, t) { return { x: lerp(a.x, b.x, t), y: lerp(a.y, b.y, t) }; }

// ============================================================
// 2. Color constants + helpers
// ============================================================
var COLORS = {
  bg: '#050509',
  panel: '#0a0e14',
  panelBorder: '#21262d',
  bone: '#c8d6e5',
  boneDim: '#576574',
  node: '#c9d1d9',
  text: '#c9d1d9',
  textDim: '#6b7280',
  label: '#00d4ff',
  heatHot: [255, 60, 40],
  heatWarm: [255, 165, 0],
  heatCool: [40, 120, 200],
  connectionGlow: '#00cec9',
  connectionDim: '#2d3436',
  forceArrow: '#fdcb6e',
  forceBlocked: '#d63031',
  forceFlowing: '#00b894',
  plumbLine: '#6b7280',
  landmarkAligned: '#55efc4',
  landmarkOff: '#e17055',
  accent: '#e056fd',
  sliderTrack: '#2d3436',
  sliderThumb: '#00d4ff',
};

function heatColor(t) {
  var hot = COLORS.heatHot, cool = COLORS.heatCool;
  var r = Math.round(cool[0] + (hot[0] - cool[0]) * t);
  var g = Math.round(cool[1] + (hot[1] - cool[1]) * t);
  var b = Math.round(cool[2] + (hot[2] - cool[2]) * t);
  return 'rgb(' + r + ',' + g + ',' + b + ')';
}

function heatColorAlpha(t, alpha) {
  var hot = COLORS.heatHot, cool = COLORS.heatCool;
  var r = Math.round(cool[0] + (hot[0] - cool[0]) * t);
  var g = Math.round(cool[1] + (hot[1] - cool[1]) * t);
  var b = Math.round(cool[2] + (hot[2] - cool[2]) * t);
  return 'rgba(' + r + ',' + g + ',' + b + ',' + alpha + ')';
}

// ============================================================
// 3. Drawing helpers
// ============================================================
function clearPanel(ctx, w, h) {
  ctx.fillStyle = COLORS.panel;
  ctx.fillRect(0, 0, w, h);
}

function drawLine(ctx, a, b, color, width) {
  ctx.beginPath();
  ctx.moveTo(a.x, a.y);
  ctx.lineTo(b.x, b.y);
  ctx.strokeStyle = color;
  ctx.lineWidth = width || 2;
  ctx.stroke();
}

function drawCircle(ctx, pos, radius, color) {
  ctx.beginPath();
  ctx.arc(pos.x, pos.y, radius, 0, Math.PI * 2);
  ctx.fillStyle = color;
  ctx.fill();
}

function drawGlow(ctx, pos, radius, color, alpha) {
  if (alpha === undefined) alpha = 0.5;
  var grad = ctx.createRadialGradient(pos.x, pos.y, 0, pos.x, pos.y, radius);
  grad.addColorStop(0, color.replace(')', ',' + alpha + ')').replace('rgb', 'rgba'));
  grad.addColorStop(1, 'rgba(0,0,0,0)');
  ctx.beginPath();
  ctx.arc(pos.x, pos.y, radius, 0, Math.PI * 2);
  ctx.fillStyle = grad;
  ctx.fill();
}

function drawArrow(ctx, from, to, color, width, headSize) {
  width = width || 2;
  headSize = headSize || 8;
  var angle = Math.atan2(to.y - from.y, to.x - from.x);
  ctx.beginPath();
  ctx.moveTo(from.x, from.y);
  ctx.lineTo(to.x, to.y);
  ctx.strokeStyle = color;
  ctx.lineWidth = width;
  ctx.stroke();
  ctx.beginPath();
  ctx.moveTo(to.x, to.y);
  ctx.lineTo(to.x - headSize * Math.cos(angle - Math.PI / 6), to.y - headSize * Math.sin(angle - Math.PI / 6));
  ctx.lineTo(to.x - headSize * Math.cos(angle + Math.PI / 6), to.y - headSize * Math.sin(angle + Math.PI / 6));
  ctx.closePath();
  ctx.fillStyle = color;
  ctx.fill();
}

function drawLabel(ctx, text, x, y, fontSize, color) {
  ctx.font = (fontSize || 14) + "px 'Courier New', Consolas, monospace";
  ctx.fillStyle = color || COLORS.text;
  ctx.textAlign = 'center';
  ctx.fillText(text, x, y);
}

function drawPanelTitle(ctx, title, subtitle, w, h, pos) {
  var pad = 12;
  ctx.font = "bold 18px 'Courier New', Consolas, monospace";
  ctx.fillStyle = COLORS.label;
  if (pos === 'tl') { ctx.textAlign = 'left';  ctx.fillText(title, pad, 26); }
  else if (pos === 'tr') { ctx.textAlign = 'right'; ctx.fillText(title, w - pad, 26); }
  else if (pos === 'bl') { ctx.textAlign = 'left';  ctx.fillText(title, pad, h - pad); }
  else if (pos === 'br') { ctx.textAlign = 'right'; ctx.fillText(title, w - pad, h - pad); }
  if (subtitle) {
    ctx.font = "bold 20px sans-serif";
    ctx.fillStyle = COLORS.textDim;
    if (pos === 'tl') { ctx.textAlign = 'right'; ctx.fillText(subtitle, w - pad, h - pad); }
    else if (pos === 'tr') { ctx.textAlign = 'left';  ctx.fillText(subtitle, pad, h - pad); }
    else if (pos === 'bl') { ctx.textAlign = 'right'; ctx.fillText(subtitle, w - pad, 26); }
    else if (pos === 'br') { ctx.textAlign = 'left';  ctx.fillText(subtitle, pad, 26); }
  }
}

function drawDashedLine(ctx, a, b, color, width, dash) {
  ctx.beginPath();
  ctx.setLineDash(dash || [4, 4]);
  ctx.moveTo(a.x, a.y);
  ctx.lineTo(b.x, b.y);
  ctx.strokeStyle = color;
  ctx.lineWidth = width || 1;
  ctx.stroke();
  ctx.setLineDash([]);
}

// ============================================================
// 4. Mannequin topology
// ============================================================
var NODES = [
  'head', 'neck',
  'shoulderL', 'shoulderR', 'shoulderMid',
  'elbowL', 'elbowR',
  'handL', 'handR',
  'spine', 'core',
  'hipL', 'hipR', 'hipMid',
  'kneeL', 'kneeR',
  'ankleL', 'ankleR', 'ankleMid',
];

var BONES = [
  ['head', 'neck'],
  ['neck', 'shoulderL'], ['neck', 'shoulderR'],
  ['shoulderL', 'elbowL'], ['shoulderR', 'elbowR'],
  ['elbowL', 'handL'], ['elbowR', 'handR'],
  ['neck', 'spine'], ['spine', 'core'],
  ['core', 'hipL'], ['core', 'hipR'],
  ['hipL', 'kneeL'], ['hipR', 'kneeR'],
  ['kneeL', 'ankleL'], ['kneeR', 'ankleR'],
];

var MIDPOINTS = {
  shoulderMid: ['shoulderL', 'shoulderR'],
  hipMid: ['hipL', 'hipR'],
  ankleMid: ['ankleL', 'ankleR'],
};

var REGIONS = {
  head: ['head'],
  neck: ['neck'],
  shoulderR: ['shoulderR'],
  shoulderL: ['shoulderL'],
  armR: ['elbowR', 'handR'],
  armL: ['elbowL', 'handL'],
  upperBack: ['spine'],
  core: ['core'],
  hipR: ['hipR'],
  hipL: ['hipL'],
  legR: ['kneeR', 'ankleR'],
  legL: ['kneeL', 'ankleL'],
};

var CONNECTION_PAIRS = [
  { from: 'shoulderL', to: 'hipR', label: 'L shoulder→R hip' },
  { from: 'shoulderR', to: 'hipL', label: 'R shoulder→L hip' },
  { from: 'elbowL', to: 'kneeL', label: 'L elbow→L knee' },
  { from: 'elbowR', to: 'kneeR', label: 'R elbow→R knee' },
  { from: 'handL', to: 'handR', label: 'hand↔hand' },
  { from: 'shoulderL', to: 'hipL', label: 'L column' },
  { from: 'shoulderR', to: 'hipR', label: 'R column' },
];

var SPINE_PATH = ['head', 'neck', 'spine', 'core'];
var ARCH_PATH = ['ankleL', 'kneeL', 'hipL', 'hipR', 'kneeR', 'ankleR'];
var FORCE_PATH = ['shoulderR', 'neck', 'spine', 'core', 'hipR', 'kneeR', 'ankleR'];
var LANDMARKS = ['head', 'shoulderMid', 'hipMid', 'ankleMid'];

function scalePositions(positions, w, h) {
  var margin = { top: 60, bottom: 20, left: 40, right: 40 };
  var drawW = w - margin.left - margin.right;
  var drawH = h - margin.top - margin.bottom;
  var scaled = {};
  for (var key in positions) {
    scaled[key] = {
      x: margin.left + positions[key].x * drawW,
      y: margin.top + positions[key].y * drawH,
    };
  }
  for (var mid in MIDPOINTS) {
    var pair = MIDPOINTS[mid];
    if (scaled[pair[0]] && scaled[pair[1]]) {
      scaled[mid] = {
        x: (scaled[pair[0]].x + scaled[pair[1]].x) / 2,
        y: (scaled[pair[0]].y + scaled[pair[1]].y) / 2,
      };
    }
  }
  return scaled;
}

function drawMannequin(ctx, positions, opts) {
  opts = opts || {};
  var boneColor = opts.boneColor || COLORS.boneDim;
  var nodeColor = opts.nodeColor || COLORS.node;
  var boneWidth = opts.boneWidth || 2;
  var nodeRadius = opts.nodeRadius || 3;
  var showNodes = opts.showNodes !== false;
  var midpointKeys = Object.keys(MIDPOINTS);

  for (var i = 0; i < BONES.length; i++) {
    var a = BONES[i][0], b = BONES[i][1];
    if (positions[a] && positions[b]) drawLine(ctx, positions[a], positions[b], boneColor, boneWidth);
  }
  if (showNodes) {
    for (var j = 0; j < NODES.length; j++) {
      if (positions[NODES[j]] && midpointKeys.indexOf(NODES[j]) === -1) {
        drawCircle(ctx, positions[NODES[j]], nodeRadius, nodeColor);
      }
    }
  }
}

// ============================================================
// 5. State interpolation
// ============================================================
function computeState(scenario, t) {
  t = clamp(t, 0, 1);
  var kfs = scenario.keyframes;
  var lo = kfs[0], hi = kfs[kfs.length - 1];
  for (var i = 0; i < kfs.length - 1; i++) {
    if (t >= kfs[i].t && t <= kfs[i + 1].t) {
      lo = kfs[i]; hi = kfs[i + 1]; break;
    }
  }
  var segLen = hi.t - lo.t;
  var lt = segLen === 0 ? 0 : (t - lo.t) / segLen;

  return {
    t: t,
    label: lt < 0.5 ? lo.label : hi.label,
    description: lt < 0.5 ? lo.description : hi.description,
    positions: lerpPositions(lo.positions, hi.positions, lt),
    tension: lerpObject(lo.tension, hi.tension, lt),
    connections: lerpObject(lo.connections, hi.connections, lt),
    forceEfficiency: lerp(lo.forceEfficiency, hi.forceEfficiency, lt),
    forceAccumulation: lerpObject(lo.forceAccumulation, hi.forceAccumulation, lt),
  };
}

function lerpPositions(a, b, t) {
  var result = {};
  for (var key in a) result[key] = lerpPos(a[key], b[key], t);
  return result;
}

function lerpObject(a, b, t) {
  var result = {}, keys = {}, k;
  for (k in a) keys[k] = true;
  for (k in b) keys[k] = true;
  for (k in keys) result[k] = lerp(a[k] || 0, b[k] || 0, t);
  return result;
}

// ============================================================
// 6. Scenario data (standing only)
// ============================================================
var standing = {
  name: 'Standing', nameJP: '立位',
  keyframes: [
    {
      t: 0.0, label: 'Unorganized', description: 'Mess, unaligned, unable to absorb or generate force',
      positions: { head:{x:0.64,y:0.05}, neck:{x:0.58,y:0.12}, shoulderL:{x:0.42,y:0.15}, shoulderR:{x:0.78,y:0.13}, elbowL:{x:0.30,y:0.30}, elbowR:{x:0.82,y:0.27}, handL:{x:0.26,y:0.42}, handR:{x:0.84,y:0.39}, spine:{x:0.56,y:0.28}, core:{x:0.46,y:0.42}, hipL:{x:0.30,y:0.53}, hipR:{x:0.48,y:0.50}, kneeL:{x:0.41,y:0.70}, kneeR:{x:0.59,y:0.69}, ankleL:{x:0.40,y:0.90}, ankleR:{x:0.60,y:0.89} },
      tension: { head:0.8, neck:0.9, shoulderR:0.95, shoulderL:0.7, armR:0.8, armL:0.4, upperBack:0.6, core:0.3, hipR:0.65, hipL:0.35, legR:0.7, legL:0.4 },
      connections: { 'shoulderL-hipR':0.05, 'shoulderR-hipL':0.05, 'elbowL-kneeL':0.0, 'elbowR-kneeR':0.0, 'handL-handR':0.0, 'shoulderL-hipL':0.1, 'shoulderR-hipR':0.1, 'spine':0.05, 'arch':0.0 },
      forceEfficiency: 0.1,
      forceAccumulation: { shoulderR:0.9, neck:0.8, spine:0.4, core:0.1, hipR:0.0, kneeR:0.0, ankleR:0.0 },
    },
    {
      t: 0.2, label: 'Head Pulled Up', description: 'Ballet "suspended from invisible string", Taichi "uplifting through baihui", the body elongates',
      positions: { head:{x:0.58,y:0.03}, neck:{x:0.55,y:0.10}, shoulderL:{x:0.40,y:0.15}, shoulderR:{x:0.74,y:0.14}, elbowL:{x:0.30,y:0.30}, elbowR:{x:0.76,y:0.28}, handL:{x:0.27,y:0.42}, handR:{x:0.78,y:0.40}, spine:{x:0.54,y:0.26}, core:{x:0.49,y:0.41}, hipL:{x:0.35,y:0.52}, hipR:{x:0.53,y:0.51}, kneeL:{x:0.41,y:0.70}, kneeR:{x:0.59,y:0.69}, ankleL:{x:0.41,y:0.90}, ankleR:{x:0.59,y:0.89} },
      tension: { head:0.5, neck:0.85, shoulderR:0.85, shoulderL:0.45, armR:0.6, armL:0.25, upperBack:0.7, core:0.35, hipR:0.7, hipL:0.3, legR:0.7, legL:0.35 },
      connections: { 'shoulderL-hipR':0.1, 'shoulderR-hipL':0.1, 'elbowL-kneeL':0.02, 'elbowR-kneeR':0.02, 'handL-handR':0.02, 'shoulderL-hipL':0.15, 'shoulderR-hipR':0.15, 'spine':0.08, 'arch':0.0 },
      forceEfficiency: 0.18,
      forceAccumulation: { shoulderR:0.85, neck:0.7, spine:0.4, core:0.15, hipR:0.02, kneeR:0.0, ankleR:0.0 },
    },
    {
      t: 0.4, label: 'Drop Shoulders', description: 'Shoulders drop in sockets, upper body tension drains',
      positions: { head:{x:0.53,y:0.03}, neck:{x:0.52,y:0.10}, shoulderL:{x:0.38,y:0.18}, shoulderR:{x:0.66,y:0.18}, elbowL:{x:0.30,y:0.32}, elbowR:{x:0.70,y:0.32}, handL:{x:0.28,y:0.44}, handR:{x:0.72,y:0.44}, spine:{x:0.51,y:0.25}, core:{x:0.50,y:0.41}, hipL:{x:0.40,y:0.52}, hipR:{x:0.56,y:0.52}, kneeL:{x:0.43,y:0.70}, kneeR:{x:0.57,y:0.70}, ankleL:{x:0.43,y:0.90}, ankleR:{x:0.57,y:0.90} },
      tension: { head:0.25, neck:0.7, shoulderR:0.7, shoulderL:0.2, armR:0.35, armL:0.1, upperBack:0.65, core:0.5, hipR:0.65, hipL:0.25, legR:0.65, legL:0.3 },
      connections: { 'shoulderL-hipR':0.2, 'shoulderR-hipL':0.2, 'elbowL-kneeL':0.1, 'elbowR-kneeR':0.1, 'handL-handR':0.1, 'shoulderL-hipL':0.3, 'shoulderR-hipR':0.3, 'spine':0.15, 'arch':0.0 },
      forceEfficiency: 0.35,
      forceAccumulation: { shoulderR:0.65, neck:0.45, spine:0.5, core:0.3, hipR:0.1, kneeR:0.02, ankleR:0.0 },
    },
    {
      t: 0.6, label: 'Wrap Diaphragm', description: 'The "bell shape" that is the diaphragm wraps downward, unifying the core',
      positions: { head:{x:0.50,y:0.03}, neck:{x:0.50,y:0.10}, shoulderL:{x:0.37,y:0.18}, shoulderR:{x:0.63,y:0.18}, elbowL:{x:0.32,y:0.32}, elbowR:{x:0.68,y:0.32}, handL:{x:0.30,y:0.44}, handR:{x:0.70,y:0.44}, spine:{x:0.50,y:0.25}, core:{x:0.50,y:0.40}, hipL:{x:0.43,y:0.52}, hipR:{x:0.57,y:0.52}, kneeL:{x:0.44,y:0.71}, kneeR:{x:0.56,y:0.71}, ankleL:{x:0.44,y:0.91}, ankleR:{x:0.56,y:0.91} },
      tension: { head:0.1, neck:0.4, shoulderR:0.4, shoulderL:0.08, armR:0.15, armL:0.05, upperBack:0.35, core:0.55, hipR:0.45, hipL:0.12, legR:0.45, legL:0.2 },
      connections: { 'shoulderL-hipR':0.5, 'shoulderR-hipL':0.5, 'elbowL-kneeL':0.3, 'elbowR-kneeR':0.3, 'handL-handR':0.2, 'shoulderL-hipL':0.5, 'shoulderR-hipR':0.5, 'spine':0.45, 'arch':0.0 },
      forceEfficiency: 0.6,
      forceAccumulation: { shoulderR:0.4, neck:0.35, spine:0.5, core:0.6, hipR:0.3, kneeR:0.15, ankleR:0.05 },
    },
    {
      t: 0.8, label: 'Engage Hips', description: 'Pelvic adjustment, feeling the leg "arch", connecting upper and lower body',
      positions: { head:{x:0.50,y:0.03}, neck:{x:0.50,y:0.10}, shoulderL:{x:0.37,y:0.17}, shoulderR:{x:0.63,y:0.17}, elbowL:{x:0.33,y:0.31}, elbowR:{x:0.67,y:0.31}, handL:{x:0.31,y:0.43}, handR:{x:0.69,y:0.43}, spine:{x:0.50,y:0.24}, core:{x:0.50,y:0.39}, hipL:{x:0.44,y:0.52}, hipR:{x:0.56,y:0.52}, kneeL:{x:0.45,y:0.72}, kneeR:{x:0.55,y:0.72}, ankleL:{x:0.45,y:0.92}, ankleR:{x:0.55,y:0.92} },
      tension: { head:0.06, neck:0.12, shoulderR:0.15, shoulderL:0.05, armR:0.08, armL:0.04, upperBack:0.15, core:0.35, hipR:0.25, hipL:0.06, legR:0.3, legL:0.12 },
      connections: { 'shoulderL-hipR':0.8, 'shoulderR-hipL':0.8, 'elbowL-kneeL':0.6, 'elbowR-kneeR':0.6, 'handL-handR':0.5, 'shoulderL-hipL':0.75, 'shoulderR-hipR':0.75, 'spine':0.8, 'arch':0.8 },
      forceEfficiency: 0.85,
      forceAccumulation: { shoulderR:0.2, neck:0.2, spine:0.35, core:0.55, hipR:0.55, kneeR:0.5, ankleR:0.6 },
    },
    {
      t: 1.0, label: 'Tsukuri', description: 'Fully "Built", organized, ideally no wasted energy',
      positions: { head:{x:0.50,y:0.03}, neck:{x:0.50,y:0.10}, shoulderL:{x:0.38,y:0.17}, shoulderR:{x:0.62,y:0.17}, elbowL:{x:0.34,y:0.31}, elbowR:{x:0.66,y:0.31}, handL:{x:0.32,y:0.43}, handR:{x:0.68,y:0.43}, spine:{x:0.50,y:0.24}, core:{x:0.50,y:0.39}, hipL:{x:0.45,y:0.52}, hipR:{x:0.55,y:0.52}, kneeL:{x:0.45,y:0.72}, kneeR:{x:0.55,y:0.72}, ankleL:{x:0.45,y:0.92}, ankleR:{x:0.55,y:0.92} },
      tension: { head:0.05, neck:0.05, shoulderR:0.08, shoulderL:0.03, armR:0.05, armL:0.03, upperBack:0.1, core:0.3, hipR:0.06, hipL:0.03, legR:0.1, legL:0.05 },
      connections: { 'shoulderL-hipR':0.95, 'shoulderR-hipL':0.95, 'elbowL-kneeL':0.8, 'elbowR-kneeR':0.8, 'handL-handR':0.7, 'shoulderL-hipL':0.9, 'shoulderR-hipR':0.9, 'spine':0.95, 'arch':0.95 },
      forceEfficiency: 0.95,
      forceAccumulation: { shoulderR:0.15, neck:0.15, spine:0.3, core:0.5, hipR:0.6, kneeR:0.7, ankleR:0.9 },
    },
  ],
};

// ============================================================
// 7. Panel renderers
// ============================================================
var REGION_RADIUS = { head:22, neck:16, shoulderR:28, shoulderL:28, armR:20, armL:20, upperBack:24, core:30, hipR:26, hipL:26, legR:22, legL:22 };

function renderTensionHeatmap(ctx, state, w, h) {
  clearPanel(ctx, w, h);
  drawPanelTitle(ctx, 'Tension Economy', '\u7121\u99C4\u3092\u7701\u304F', w, h, 'tl');
  var positions = scalePositions(state.positions, w, h);

  for (var region in REGIONS) {
    var tension = state.tension[region] || 0;
    var radius = REGION_RADIUS[region] || 20;
    var nodeNames = REGIONS[region];
    for (var n = 0; n < nodeNames.length; n++) {
      var pos = positions[nodeNames[n]];
      if (!pos) continue;
      var r = radius + tension * 15;
      var grad = ctx.createRadialGradient(pos.x, pos.y, 0, pos.x, pos.y, r);
      grad.addColorStop(0, heatColorAlpha(tension, 0.5 + tension * 0.3));
      grad.addColorStop(0.6, heatColorAlpha(tension, 0.2 * tension));
      grad.addColorStop(1, 'rgba(0,0,0,0)');
      ctx.beginPath();
      ctx.arc(pos.x, pos.y, r, 0, Math.PI * 2);
      ctx.fillStyle = grad;
      ctx.fill();
    }
  }
  drawMannequin(ctx, positions, { boneColor: COLORS.bone + '90', nodeRadius: 2.5 });
}

function panelScale(w) { return Math.min(w / 400, 1); }

function renderConnectionLines(ctx, state, w, h, time) {
  clearPanel(ctx, w, h);
  drawPanelTitle(ctx, 'Connections', '\u9AD4\u306E\u7D71\u4E00', w, h, 'tr');
  var positions = scalePositions(state.positions, w, h);
  var sc = panelScale(w);

  drawMannequin(ctx, positions, { boneColor: COLORS.boneDim + '60', nodeColor: COLORS.node + '40', nodeRadius: 2 * sc });

  var corePos = positions.core;
  if (!corePos) return;

  var archStrength = state.connections['arch'] || 0;
  if (archStrength > 0.02) drawArch(ctx, positions, archStrength, time, sc);

  var spineStrength = state.connections['spine'] || 0;
  if (spineStrength > 0.02) drawSpine(ctx, positions, spineStrength, time, sc);

  for (var p = 0; p < CONNECTION_PAIRS.length; p++) {
    var pair = CONNECTION_PAIRS[p];
    var fromPos = positions[pair.from];
    var toPos = positions[pair.to];
    if (!fromPos || !toPos) continue;
    var key = pair.from + '-' + pair.to;
    var strength = state.connections[key] || 0;
    if (strength < 0.02) continue;

    var cpx = corePos.x + (fromPos.x + toPos.x - 2 * corePos.x) * 0.3;
    var cpy = corePos.y + (fromPos.y + toPos.y - 2 * corePos.y) * 0.3;
    var pulse = strength > 0.5 ? 1 + 0.15 * Math.sin(time * 3 + strength * 10) : 1;
    var alpha = strength * 0.8 * pulse;
    var width = (1 + strength * 3) * sc;

    if (strength > 0.3) {
      ctx.beginPath();
      ctx.moveTo(fromPos.x, fromPos.y);
      ctx.quadraticCurveTo(cpx, cpy, toPos.x, toPos.y);
      ctx.strokeStyle = COLORS.connectionGlow + Math.round(alpha * 0.3 * 255).toString(16).padStart(2, '0');
      ctx.lineWidth = width + 4 * sc;
      ctx.stroke();
    }

    ctx.beginPath();
    ctx.moveTo(fromPos.x, fromPos.y);
    ctx.quadraticCurveTo(cpx, cpy, toPos.x, toPos.y);
    ctx.strokeStyle = COLORS.connectionGlow + Math.round(alpha * 255).toString(16).padStart(2, '0');
    ctx.lineWidth = width;
    ctx.stroke();

    var dotR = (3 + strength * 2) * sc;
    var dotAlpha = Math.round(strength * 200).toString(16).padStart(2, '0');
    ctx.beginPath();
    ctx.arc(fromPos.x, fromPos.y, dotR, 0, Math.PI * 2);
    ctx.fillStyle = COLORS.connectionGlow + dotAlpha;
    ctx.fill();
    ctx.beginPath();
    ctx.arc(toPos.x, toPos.y, dotR, 0, Math.PI * 2);
    ctx.fillStyle = COLORS.connectionGlow + dotAlpha;
    ctx.fill();
  }
}

function drawSmoothCurve(ctx, pts) {
  if (pts.length < 2) return;
  ctx.moveTo(pts[0].x, pts[0].y);
  if (pts.length === 2) { ctx.lineTo(pts[1].x, pts[1].y); return; }
  var mid01x = (pts[0].x + pts[1].x) / 2;
  var mid01y = (pts[0].y + pts[1].y) / 2;
  ctx.quadraticCurveTo(pts[0].x, pts[0].y, mid01x, mid01y);
  for (var i = 1; i < pts.length - 1; i++) {
    var midx = (pts[i].x + pts[i + 1].x) / 2;
    var midy = (pts[i].y + pts[i + 1].y) / 2;
    ctx.quadraticCurveTo(pts[i].x, pts[i].y, midx, midy);
  }
  ctx.lineTo(pts[pts.length - 1].x, pts[pts.length - 1].y);
}

function drawSpine(ctx, positions, strength, time, sc) {
  sc = sc || 1;
  var pts = SPINE_PATH.map(function(name) { return positions[name]; }).filter(Boolean);
  if (pts.length < 2) return;
  var pulse = strength > 0.5 ? 1 + 0.1 * Math.sin(time * 2) : 1;
  var alpha = strength * 0.85 * pulse;
  var width = (1.5 + strength * 3) * sc;
  var color = '#fdcb6e';

  if (strength > 0.3) {
    ctx.beginPath();
    drawSmoothCurve(ctx, pts);
    ctx.strokeStyle = color + Math.round(alpha * 0.25 * 255).toString(16).padStart(2, '0');
    ctx.lineWidth = width + 6 * sc;
    ctx.lineCap = 'round';
    ctx.stroke();
  }

  ctx.beginPath();
  drawSmoothCurve(ctx, pts);
  ctx.strokeStyle = color + Math.round(alpha * 255).toString(16).padStart(2, '0');
  ctx.lineWidth = width;
  ctx.lineCap = 'round';
  ctx.lineJoin = 'round';
  ctx.stroke();

  var dotAlpha = Math.round(strength * 220).toString(16).padStart(2, '0');
  for (var j = 0; j < pts.length; j++) {
    ctx.beginPath();
    ctx.arc(pts[j].x, pts[j].y, (3 + strength * 2) * sc, 0, Math.PI * 2);
    ctx.fillStyle = color + dotAlpha;
    ctx.fill();
  }

  if (strength > 0.4) {
    var midPt = pts[Math.floor(pts.length / 2)];
    ctx.font = Math.round(13 * sc) + "px 'Courier New', Consolas, monospace";
    ctx.fillStyle = color + Math.round(alpha * 200).toString(16).padStart(2, '0');
    ctx.textAlign = 'center';
    ctx.fillText('spine', midPt.x + 24 * sc, midPt.y);
  }
}

function drawArch(ctx, positions, strength, time, sc) {
  sc = sc || 1;
  var pts = ARCH_PATH.map(function(name) { return positions[name]; }).filter(Boolean);
  if (pts.length < 4) return;
  var pulse = strength > 0.5 ? 1 + 0.1 * Math.sin(time * 2.5) : 1;
  var alpha = strength * 0.85 * pulse;
  var width = (1.5 + strength * 3) * sc;

  var archPts = pts.map(function(p, i) {
    if (i === 2 || i === 3) return { x: p.x, y: p.y - strength * 12 * sc };
    return p;
  });

  if (strength > 0.3) {
    ctx.beginPath();
    drawSmoothCurve(ctx, archPts);
    ctx.strokeStyle = '#e056fd' + Math.round(alpha * 0.25 * 255).toString(16).padStart(2, '0');
    ctx.lineWidth = width + 6 * sc;
    ctx.stroke();
  }

  ctx.beginPath();
  drawSmoothCurve(ctx, archPts);
  ctx.strokeStyle = '#e056fd' + Math.round(alpha * 255).toString(16).padStart(2, '0');
  ctx.lineWidth = width;
  ctx.lineCap = 'round';
  ctx.lineJoin = 'round';
  ctx.stroke();

  var dotAlpha = Math.round(strength * 220).toString(16).padStart(2, '0');
  for (var j = 0; j < pts.length; j++) {
    ctx.beginPath();
    ctx.arc(pts[j].x, pts[j].y, (3 + strength * 2.5) * sc, 0, Math.PI * 2);
    ctx.fillStyle = '#e056fd' + dotAlpha;
    ctx.fill();
  }

  if (strength > 0.4) {
    var midX = (archPts[2].x + archPts[3].x) / 2;
    var midY = (archPts[2].y + archPts[3].y) / 2 - 14 * sc;
    ctx.font = Math.round(13 * sc) + "px 'Courier New', Consolas, monospace";
    ctx.fillStyle = '#e056fd' + Math.round(alpha * 200).toString(16).padStart(2, '0');
    ctx.textAlign = 'center';
    ctx.fillText('arch', midX, midY);
  }
}

function renderForceFlow(ctx, state, w, h, time) {
  clearPanel(ctx, w, h);
  drawPanelTitle(ctx, 'Ground Path', '\u5730\u9762\u3078\u306E\u9053', w, h, 'bl');
  var positions = scalePositions(state.positions, w, h);

  drawMannequin(ctx, positions, { boneColor: COLORS.boneDim + '50', nodeColor: COLORS.node + '30', nodeRadius: 2 });

  var efficiency = state.forceEfficiency;
  var accum = state.forceAccumulation;
  var shoulderR = positions.shoulderR;
  if (!shoulderR) return;

  var pushStart = { x: shoulderR.x + 50, y: shoulderR.y - 10 };
  drawArrow(ctx, pushStart, shoulderR, COLORS.forceArrow, 2.5, 10);

  for (var i = 0; i < FORCE_PATH.length - 1; i++) {
    var fromName = FORCE_PATH[i];
    var toName = FORCE_PATH[i + 1];
    var fromPos = positions[fromName];
    var toPos = positions[toName];
    if (!fromPos || !toPos) continue;

    var fromAccum = accum[fromName] || 0;
    var toAccum = accum[toName] || 0;
    var segFlow = (fromAccum + toAccum) / 2;

    var flowColor = segFlow > 0.1
      ? lerpColor(COLORS.forceBlocked, COLORS.forceFlowing, efficiency)
      : COLORS.boneDim + '40';

    var dashOffset = time * 60 * efficiency;
    ctx.beginPath();
    ctx.setLineDash([6, 4]);
    ctx.lineDashOffset = -dashOffset;
    ctx.moveTo(fromPos.x, fromPos.y);
    ctx.lineTo(toPos.x, toPos.y);
    ctx.strokeStyle = flowColor;
    ctx.lineWidth = 1.5 + segFlow * 2.5;
    ctx.stroke();
    ctx.setLineDash([]);

    if (fromAccum > 0.2) {
      var blocked = fromAccum * (1 - efficiency);
      if (blocked > 0.1) {
        var grad = ctx.createRadialGradient(fromPos.x, fromPos.y, 0, fromPos.x, fromPos.y, 12 + blocked * 15);
        grad.addColorStop(0, 'rgba(214,48,49,' + (blocked * 0.5) + ')');
        grad.addColorStop(1, 'rgba(214,48,49,0)');
        ctx.beginPath();
        ctx.arc(fromPos.x, fromPos.y, 12 + blocked * 15, 0, Math.PI * 2);
        ctx.fillStyle = grad;
        ctx.fill();
      }
    }
  }

  var lastNode = FORCE_PATH[FORCE_PATH.length - 1];
  var lastPos = positions[lastNode];
  if (lastPos) {
    var groundFlow = accum[lastNode] || 0;
    if (groundFlow > 0.1) {
      var groundPt = { x: lastPos.x, y: lastPos.y + 20 };
      drawArrow(ctx, lastPos, groundPt, COLORS.forceFlowing + Math.round(groundFlow * 200).toString(16).padStart(2, '0'), 2, 8);
      ctx.beginPath();
      ctx.moveTo(lastPos.x - 20, groundPt.y + 4);
      ctx.lineTo(lastPos.x + 20, groundPt.y + 4);
      ctx.strokeStyle = COLORS.forceFlowing + '60';
      ctx.lineWidth = 2;
      ctx.stroke();
    }
  }
}

function lerpColor(hexA, hexB, t) {
  var a = hexToRgb(hexA), b = hexToRgb(hexB);
  return 'rgb(' + Math.round(lerp(a[0], b[0], t)) + ',' + Math.round(lerp(a[1], b[1], t)) + ',' + Math.round(lerp(a[2], b[2], t)) + ')';
}

function hexToRgb(hex) {
  hex = hex.replace('#', '');
  return [parseInt(hex.substring(0,2),16), parseInt(hex.substring(2,4),16), parseInt(hex.substring(4,6),16)];
}

var LANDMARK_LABELS = ['Ear', 'Shoulder', 'Hip', 'Ankle'];

function renderStacking(ctx, state, w, h) {
  clearPanel(ctx, w, h);
  drawPanelTitle(ctx, 'Stacking', '\u90E8\u4F4D\u306E\u6574\u5217', w, h, 'br');
  var positions = scalePositions(state.positions, w, h);

  var ankleMid = positions.ankleMid;
  if (!ankleMid) return;

  drawDashedLine(ctx, { x: ankleMid.x, y: 55 }, { x: ankleMid.x, y: h - 10 }, COLORS.plumbLine, 1, [6, 4]);

  var landmarkPositions = LANDMARKS.map(function(name) { return positions[name]; });

  for (var i = 0; i < landmarkPositions.length; i++) {
    var pos = landmarkPositions[i];
    if (!pos) continue;
    var deviation = Math.abs(pos.x - ankleMid.x);
    var maxDeviation = w * 0.15;
    var normalizedDev = Math.min(deviation / maxDeviation, 1);

    var r = Math.round(lerp(85, 225, normalizedDev));
    var g = Math.round(lerp(239, 112, normalizedDev));
    var b = Math.round(lerp(196, 85, normalizedDev));
    var color = 'rgb(' + r + ',' + g + ',' + b + ')';

    if (normalizedDev > 0.05) {
      drawLine(ctx, pos, { x: ankleMid.x, y: pos.y }, color + '60', 1);
    }
    drawCircle(ctx, pos, 6 + normalizedDev * 4, color);
    drawLabel(ctx, LANDMARK_LABELS[i], pos.x + (pos.x > w / 2 ? 30 : -30), pos.y + 4, Math.max(14, Math.round(14 * panelScale(w))), COLORS.textDim);
  }

  var totalDev = 0;
  for (var j = 0; j < landmarkPositions.length; j++) {
    if (landmarkPositions[j]) totalDev += Math.abs(landmarkPositions[j].x - ankleMid.x);
  }
  var maxTotal = w * 0.15 * 4;
  var alignScore = Math.round((1 - Math.min(totalDev / maxTotal, 1)) * 100);
  drawLabel(ctx, alignScore + '%', w / 2, h - 8, 12, COLORS.textDim);
}

// ============================================================
// 8. Bootstrap
// ============================================================
var scenario = standing;

var slider = document.getElementById('by-slider');
var sliderLabel = document.getElementById('by-slider-label');
var stepsContainer = document.getElementById('by-steps');
var autoplayBtn = document.getElementById('by-autoplay');
var playIcon = autoplayBtn.querySelector('.by-play-icon');
var canvases = [
  document.getElementById('by-canvas-tension'),
  document.getElementById('by-canvas-connections'),
  document.getElementById('by-canvas-force'),
  document.getElementById('by-canvas-stacking'),
];
var ctxs = canvases.map(function(c) { return c.getContext('2d'); });

var panels = [
  { render: renderTensionHeatmap, usesTime: false },
  { render: renderConnectionLines, usesTime: true },
  { render: renderForceFlow, usesTime: true },
  { render: renderStacking, usesTime: false },
];

// --- Sizing ---
function resize() {
  canvases.forEach(function(canvas, i) {
    var rect = canvas.parentElement.getBoundingClientRect();
    var dpr = window.devicePixelRatio || 1;
    canvas.width = rect.width * dpr;
    canvas.height = rect.height * dpr;
    canvas.style.width = rect.width + 'px';
    canvas.style.height = rect.height + 'px';
    ctxs[i].setTransform(dpr, 0, 0, dpr, 0, 0);
    canvas.logicalWidth = rect.width;
    canvas.logicalHeight = rect.height;
  });
}

// --- Step buttons ---
var STEP_COLORS = ['#e53935','#ef6a5a','#fb8c00','#fdd835','#66bb6a','#a5d6a7'];
var stepBtns = [];

function buildSteps() {
  stepsContainer.innerHTML = '';
  stepBtns = [];
  for (var i = 0; i < scenario.keyframes.length; i++) {
    var kf = scenario.keyframes[i];
    (function(kf) {
      var btn = document.createElement('button');
      btn.className = 'by-step-btn';
      btn.textContent = kf.label;
      btn.title = kf.description;
      btn.setAttribute('aria-label', kf.label + ': ' + kf.description);
      btn.addEventListener('click', function() {
        setAutoplayState(false);
        animateSliderTo(kf.t);
      });
      stepsContainer.appendChild(btn);
      stepBtns.push(btn);
    })(kf);
  }
}

// --- Smooth slider animation ---
var animTarget = null;
var animStart = null;
var animFrom = null;
var ANIM_DURATION = 600;

function animateSliderTo(target) {
  animTarget = target;
  animStart = performance.now();
  animFrom = parseFloat(slider.value);
}

function updateAnimatedSlider(now) {
  if (animTarget === null) return;
  var elapsed = now - animStart;
  var progress = Math.min(elapsed / ANIM_DURATION, 1);
  slider.value = lerp(animFrom, animTarget, easeInOut(progress));
  if (progress >= 1) animTarget = null;
}

// --- Auto-play ---
var autoplayActive = true;
var userPaused = false;
var AUTOPLAY_SPEED = 0.08;
var LOOP_PAUSE_MS = 3000;
var lastAutoplayTime = 0;
var loopPauseUntil = 0;
var autoplayValue = 0;

function setAutoplayState(playing) {
  if (autoplayActive === playing) return;
  autoplayActive = playing;
  autoplayBtn.classList.toggle('playing', playing);
  playIcon.textContent = playing ? '\u23F8' : '\u25B6';
  autoplayBtn.setAttribute('aria-label', playing ? 'Pause auto-advance' : 'Play auto-advance');
  if (playing) {
    lastAutoplayTime = performance.now();
    autoplayValue = parseFloat(slider.value);
  }
}

autoplayBtn.addEventListener('click', function() {
  userPaused = autoplayActive;
  loopPauseUntil = 0;
  if (!autoplayActive && parseFloat(slider.value) >= 1) slider.value = 0;
  setAutoplayState(!autoplayActive);
});

slider.addEventListener('input', function() {
  userPaused = true;
  loopPauseUntil = 0;
  setAutoplayState(false);
  animTarget = null;
});

function updateAutoplay(now) {
  if (loopPauseUntil > 0) {
    if (now >= loopPauseUntil) {
      loopPauseUntil = 0;
      autoplayValue = 0;
      slider.value = 0;
      lastAutoplayTime = now;
      setAutoplayState(true);
    }
    return;
  }
  if (!autoplayActive) return;
  if (lastAutoplayTime === 0) lastAutoplayTime = now;
  var dt = Math.min((now - lastAutoplayTime) / 1000, 0.1);
  lastAutoplayTime = now;
  autoplayValue += AUTOPLAY_SPEED * dt;
  if (autoplayValue >= 1) {
    autoplayValue = 1;
    slider.value = 1;
    setAutoplayState(false);
    if (!userPaused) loopPauseUntil = now + LOOP_PAUSE_MS;
    return;
  }
  slider.value = autoplayValue;
}

// --- Render loop ---
var startTime = performance.now();
var lastT = -1;
var lastLabelText = '';
var _lastState = null;

function render(now) {
  var time = (now - startTime) / 1000;

  updateAnimatedSlider(now);
  updateAutoplay(now);

  var t = parseFloat(slider.value);
  var tChanged = t !== lastT;
  lastT = t;

  var state = tChanged ? computeState(scenario, t) : _lastState;
  _lastState = state;

  if (tChanged) {
    var labelText = state.label + ' / ' + state.description;
    if (labelText !== lastLabelText) {
      lastLabelText = labelText;
      sliderLabel.textContent = labelText;
    }

    for (var i = 0; i < stepBtns.length; i++) {
      var kfT = scenario.keyframes[i].t;
      var passed = t >= kfT - 0.01;
      var current = Math.abs(t - kfT) < 0.01;
      var lit = passed || current;
      var c = STEP_COLORS[i] || STEP_COLORS[STEP_COLORS.length - 1];
      stepBtns[i].classList.toggle('lit', lit);
      stepBtns[i].classList.toggle('active', current);
      stepBtns[i].style.borderColor = lit ? c : '';
      stepBtns[i].style.color = lit ? c : '';
      stepBtns[i].style.background = lit ? c + '18' : '';
      stepBtns[i].setAttribute('aria-pressed', lit);
    }
  }

  for (var j = 0; j < panels.length; j++) {
    var w = canvases[j].logicalWidth;
    var h = canvases[j].logicalHeight;
    if (panels[j].usesTime || tChanged) {
      if (panels[j].usesTime) {
        panels[j].render(ctxs[j], state, w, h, time);
      } else {
        panels[j].render(ctxs[j], state, w, h);
      }
    }
  }

  requestAnimationFrame(render);
}

// --- Init ---
window.addEventListener('resize', resize);
resize();
buildSteps();
requestAnimationFrame(render);
} // end initBuildYourselfViz

initBuildYourselfViz();

// ═══ Pinch-to-Zoom on .viz-zoomable ═══
document.querySelectorAll('.viz-zoomable').forEach(function(wrapper) {
  var inner = wrapper.querySelector('.viz-container');
  var scale = 1, originX = 0, originY = 0;
  var startDist = 0, startScale = 1;
  var panStartX = 0, panStartY = 0, panX = 0, panY = 0, startPanX = 0, startPanY = 0;

  function getDist(touches) {
    var dx = touches[0].clientX - touches[1].clientX;
    var dy = touches[0].clientY - touches[1].clientY;
    return Math.sqrt(dx * dx + dy * dy);
  }

  wrapper.addEventListener('touchstart', function(e) {
    if (e.touches.length === 2) {
      e.preventDefault();
      startDist = getDist(e.touches);
      startScale = scale;
      var rect = wrapper.getBoundingClientRect();
      originX = ((e.touches[0].clientX + e.touches[1].clientX) / 2 - rect.left);
      originY = ((e.touches[0].clientY + e.touches[1].clientY) / 2 - rect.top);
      startPanX = panX; startPanY = panY;
      panStartX = (e.touches[0].clientX + e.touches[1].clientX) / 2;
      panStartY = (e.touches[0].clientY + e.touches[1].clientY) / 2;
    }
  }, { passive: false });

  wrapper.addEventListener('touchmove', function(e) {
    if (e.touches.length === 2) {
      e.preventDefault();
      var d = getDist(e.touches);
      scale = Math.min(Math.max(startScale * (d / startDist), 1), 4);
      var midX = (e.touches[0].clientX + e.touches[1].clientX) / 2;
      var midY = (e.touches[0].clientY + e.touches[1].clientY) / 2;
      panX = startPanX + (midX - panStartX);
      panY = startPanY + (midY - panStartY);
      if (scale <= 1) { panX = 0; panY = 0; }
      inner.style.transform = 'translate(' + panX + 'px,' + panY + 'px) scale(' + scale + ')';
      inner.style.transformOrigin = originX + 'px ' + originY + 'px';
    }
  }, { passive: false });

  wrapper.addEventListener('touchend', function() {
    if (scale <= 1.05) {
      scale = 1; panX = 0; panY = 0;
      inner.style.transform = '';
      inner.style.transformOrigin = '';
    }
  });

  // Double-tap to reset
  var lastTap = 0;
  wrapper.addEventListener('touchend', function(e) {
    if (e.touches.length > 0) return;
    var now = Date.now();
    if (now - lastTap < 300) {
      scale = 1; panX = 0; panY = 0;
      inner.style.transform = '';
      inner.style.transformOrigin = '';
    }
    lastTap = now;
  });
});

</script>
